# 项目数据来源与调用机制梳理

生成日期：2026-05-10

本文按业务模块梳理当前项目的数据来源地址、后端调用入口、落库或缓存位置、前端调用机制和主要回退策略。范围覆盖 FastAPI 后端、React 前端、后台 worker、QMT bridge、TradingAgents 深度分析链路和数据下载脚本。

## 1. 总体调用链

```text
React 页面
  -> frontend/src/services/api.ts
  -> /v1/... FastAPI 路由
  -> api/services/... 业务服务
  -> PostgreSQL / QMT bridge / Python 数据源库 / LLM Provider / 本地文件缓存
  -> 结果表、任务表、SSE 事件流、页面数据源治理标签
```

### 前端统一入口

| 项 | 机制 |
| --- | --- |
| API 基址 | `frontend/src/services/api.ts#getBaseUrl()` 优先使用 `VITE_API_URL`，本地开发时 loopback 页面优先走当前 Vite origin，让 `/v1` 请求通过 dev proxy；兜底为 `http://localhost:8500`。 |
| 鉴权 | `localStorage.ta-access-token` 写入 `Authorization: Bearer ...`，同时 `credentials: include` 保留 cookie/session 兼容。 |
| 超时 | 默认 15 秒；每日复盘生成等长任务单独放大超时；回测、实时监控、日志、QMT 清仓等使用 SSE 或轮询。 |
| 流式接口 | `fetch()` 直接连接 `text/event-stream`，典型接口包括 `/v1/chat/completions`、`/v1/backtests/{run_id}/stream`、`/v1/realtime/monitors/{id}/stream`、`/v1/debug/logs/stream`、`/v1/virtual-warehouse/qmt/orders/bulk-sell/{task_id}/stream`。 |

### 后端统一入口

| 项 | 机制 |
| --- | --- |
| API 注册 | `api/app.py` 聚合各路由，核心前缀为 `/v1`，后端常用端口见 `README.md`：`8500`。 |
| 主数据库 | `api/database.py` 只接受 PostgreSQL：`DATABASE_URL` 必须为 `postgresql://`、`postgresql+...` 或 `postgres://`。 |
| 策略数据库 | `api/core/strategy_db.py` 使用 `STRATEGY_DATABASE_URL`，未配置时复用 `DATABASE_URL`，同样只接受 PostgreSQL。 |
| 启动初始化 | `api/lifespan.py` 启动时初始化主库、策略库、交易日历、股票名称缓存，并按环境变量拉起后台 worker。 |
| 数据源治理 | `api/services/data_source_governance.py` 统一登记 `qmt`、`xtquant`、`qmt_bridge`、`postgresql`、`akshare`、`cache`、`synthetic`、`true_engine`、`news_cache` 等来源，并给页面输出可信度提示。 |

### 后台 worker 开关

| Worker | 开关 | 主要职责 |
| --- | --- | --- |
| QMT 账户同步 | `ENABLE_QMT_SYNC_WORKER` | 周期刷新 QMT 账户快照、同步 profile 状态。 |
| QMT 行情同步 | `ENABLE_QMT_MARKET_SYNC_WORKER` | 周期抓取指数、行情或分钟线补充数据。 |
| 回测数据自动更新 | `ENABLE_BACKTEST_AUTO_UPDATE_WORKER` | 扫描 `backtest_data_configs`，按水位增量生成下载任务。 |
| 资讯之眼 | `ENABLE_NEWS_EYE_WORKER`，默认开启 | 周期抓取外部资讯，写入 `market_news_items`。 |
| 实时监控 | `ENABLE_REALTIME_MONITOR_WORKER` | 后台扫描 running 监控实例，拉行情、判信号、写事件。 |
| 每日复盘 | `ENABLE_DAILY_REVIEW_WORKER`，默认开启 | 到点生成复盘并可推送企业微信/邮件。 |

## 2. 数据源地址总表

| 来源 | 地址或配置入口 | 主要使用模块 | 调用入口 | 落库/缓存 | 关键回退 |
| --- | --- | --- | --- | --- | --- |
| PostgreSQL 主库 | `DATABASE_URL` | 行情、报告、用户、配置、资讯、组合、QMT 快照、数据订阅 | `api/database.py#get_db`、`SessionLocal` | 所有主业务表 | 无数据库则后端启动失败。 |
| PostgreSQL 策略库 | `STRATEGY_DATABASE_URL` 或 `DATABASE_URL` | 策略管理、回测、实时监控、模拟账户 | `api/core/strategy_db.py#get_strategy_db` | `strategies`、`backtest_jobs`、`realtime_monitors` 等 | 未配独立库时复用主库。 |
| 多源行情统一视图 | 本地视图 `market_stock_daily_kline`、`market_stock_minute_kline` | 股票市场、策略回测、分析轻量 fallback、复盘 | `preferred_daily_kline_table()`、`preferred_minute_kline_table()` | 视图合并 `pub_stock_*` 与 legacy `stock_*` | 视图不存在时回退 `pub_stock_*` 或 legacy 表。 |
| QuantClass | `https://api.quantclass.cn/api/data/` | 日 K、指数、筹码、财务、部分回测数据下载 | `api/quantclass_downloader.py`、`api/quantclass_importer.py` | `data/quantclass/`，`raw_stock_daily_kline_quantclass`，`norm_stock_daily_kline`，`pub_stock_daily_kline` | 只记录产品和配置入口，不在文档写真实 key。下载后走 raw/norm/pub，不默认回写 legacy。 |
| AkShare | Python 包 `akshare`，上游由库内部封装，常见为东方财富/新浪/腾讯 | 行情补数、市场榜单、板块资金流、TradingAgents 工具 | `api/data_downloader.py`、`tradingagents/dataflows/providers/cn_akshare_provider.py`、`api/routes/market.py` | raw/norm/pub，`index_daily_data`，内存 TTL 缓存 | AkShare provider 有并发锁、熔断和 fallback chain。 |
| BaoStock | Python 包 `baostock` 登录后查询 | 日线和技术指标 fallback | `tradingagents/dataflows/providers/cn_baostock_provider.py`、`DataDownloader._fetch_daily_kline_via_baostock()` | raw/norm/pub 或分析工具返回文本 | 不支持新闻/财务时抛 `NotImplementedError`，继续 fallback。 |
| efinance | Python 包 `efinance` | 日线 fallback | `DataDownloader._fetch_daily_kline_via_efinance()` | raw/norm/pub | 不可用时任务失败或切换兼容源。 |
| QMT bridge | 用户配置中的 `bridge_base_url`，或 `QMT_BRIDGE_BASE_URL`；bridge 默认监听 `0.0.0.0:${QMT_BRIDGE_PORT:-8710}` | 股票市场实时行情、QMT 仓、实时监控、分钟线历史同步 | `scripts/qmt_bridge_server.py`、`qmt_virtual_account_service.py`、`qmt_market_data_service.py` | `qmt_account_snapshots`、`stock_minute_kline`、`index_minute_kline`、raw/norm/pub | bridge 不可达时回退快照或熔断，实时监控默认不信任 stale 快照。 |
| QMT 本地 xtquant | `QMT_USERDATA_PATH`、`QMT_HOST`、`QMT_PORT`、账号配置 | 无 bridge 时的本地账户/行情读取 | `qmt_virtual_account_service._query_qmt_snapshot_via_local_xttrader()`、`qmt_market_data_service._fetch_*_via_local_xt` | 同 QMT bridge | `xtquant` 未安装或 userdata 缺失时回退缓存/空状态。 |
| QMT 历史分钟线任务 | bridge `/history/minute/sync` 和 `/history/minute/jobs/{job_id}` | 设置页回测数据下载、全市场分钟线补数 | `api/data_downloader.py#download_minute_kline_from_qmt()` | `stock_minute_kline`，再同步到 `raw_stock_minute_kline_qmt` 和 `pub_stock_minute_kline` | `QMT_MINUTE_DATABASE_URL` 需要是 Windows 可访问的 PostgreSQL 地址。 |
| QMT 行情接口 | bridge `/market/quotes`、`/market/daily-bars`、`/market/minute-bars` | 股票市场、实时监控、日内 K 线、指数补数 | `api/services/qmt_market_data_service.py` | `stock_minute_kline`、`index_minute_kline`、`index_daily_kline` | `/market/quotes` 404 时可由分钟线最近价推导。 |
| 东方财富直连 | `https://push2his.eastmoney.com/api/qt/stock/kline/get`，北交所列表 `https://82.push2.eastmoney.com/api/qt/clist/get` | 北交所分钟线脚本 | `scripts/fetch_bj_minute_kline.py` | `stock_minute_kline` | 脚本级工具，不是主业务实时读取链路。 |
| TDX/pytdx | Python 包 `pytdx` 连接通达信行情服务器 | 历史分钟线脚本 fallback | `scripts/fetch_minute_kline_full.py` | `stock_minute_kline` | 人工脚本链路，运行后可再同步 raw/pub。 |
| Parquet/DuckDB | 本地 Parquet 根目录由 `api/services/daily_kline_parquet_store.py` 管理 | 策略回测性能缓存、数据统计、压力测试 | `load_daily_kline_slice_from_parquet()`、`write_daily_kline_parquet_cache()` | 日线 Parquet、回测 artifact Parquet/JSON | Parquet 缺失时从 PostgreSQL 拉取并刷新缓存。 |
| TradingAgents provider chain | `tradingagents/default_config.py` 中 `data_vendors` | 深度智能分析工具层 | `tradingagents/dataflows/interface.py#route_to_vendor()` | 工具返回文本，最终写 `reports` | 默认链：`cn_akshare,cn_baostock,yfinance`，注册表还含 `alpha_vantage`、stub；失败后按链路 fallback。 |
| Yahoo Finance | Python 包 `yfinance` | TradingAgents 国际数据/兜底 | `tradingagents/dataflows/providers/yfinance_provider.py` | 工具返回文本 | A 股 `.SH` 转 `.SS`，失败后继续 fallback。 |
| Alpha Vantage | `https://www.alphavantage.co/query`，`ALPHA_VANTAGE_API_KEY` | TradingAgents provider fallback，新闻/财务/指标 | `tradingagents/dataflows/alpha_vantage_common.py` | 工具返回文本 | API key 或限流失败时熔断并 fallback。 |
| LLM Provider | 默认 `TA_BASE_URL=https://api.openai.com/v1`；也支持 OpenRouter、xAI、Ollama、本地兼容地址 | 智能分析、资讯解读、每日复盘增强、策略草稿 | `tradingagents/llm_clients/factory.py`、`api/core/runtime_config.py` | `reports`、`daily_reviews`、前端 SSE | 用户配置优先；缺失/失败时部分模块走本地规则 fallback。 |
| VLM Provider | 默认 `TA_VLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4`，`TA_VLM_API_KEY` | 持仓截图识别 | `api/services/vlm_service.py` | 返回后写 `imported_portfolio_positions` | 未配置 key 则图片解析失败。 |
| 企业微信 | `https://qyapi.weixin.qq.com/cgi-bin/webhook/send`，用户配置 webhook | 定时分析、每日复盘推送 | `api/services/wecom_notification_service.py` | 无业务表，仅记录推送状态 | webhook 失败后可记录 partial/failed。 |
| SMTP 邮件 | `MAIL_HOST`/`SMTP_HOST` 等环境变量 | 登录验证码、分析报告、每日复盘邮件 | `auth_service.send_login_code()`、`email_report_service.py`、`daily_review_service.py` | 验证码写 `email_verification_codes`；邮件外发 | dev 无 SMTP 时登录码可打印到控制台。 |

## 3. 行情数据治理机制

### 3.1 表层结构

| 层 | 表/视图 | 职责 |
| --- | --- | --- |
| legacy | `stock_daily_kline`、`stock_minute_kline` | 历史主库，默认只读，不由增量治理链路直接回写。 |
| raw | `raw_stock_daily_kline_{source}`、`raw_stock_minute_kline_{source}` | 每个来源独立落地，保留来源原始口径。 |
| norm | `norm_stock_daily_kline`、`norm_stock_minute_kline` | 标准字段层，保留多来源候选。 |
| pub | `pub_stock_daily_kline`、`pub_stock_minute_kline` | 对账后的业务发布层，携带 `source`、`quality_status`、`publish_status`、`coverage_ratio`。 |
| reconciliation | `daily_kline_reconciliation_*`、`minute_kline_reconciliation_*` | 记录选择了哪个来源、冲突、缺口、覆盖率。 |
| unified | `market_stock_daily_kline`、`market_stock_minute_kline` | 业务读取视图，优先发布层，缺失时回落 legacy。 |

### 3.2 日 K 写入流程

```text
QuantClass / AkShare / BaoStock / efinance / PostgreSQL legacy
  -> ingest_raw_daily_rows(source=...)
  -> raw_stock_daily_kline_{source}
  -> norm_stock_daily_kline
  -> reconcile_daily_trade_dates()
  -> pub_stock_daily_kline
  -> market_stock_daily_kline
```

日 K 来源优先级在 `api/services/market_data_pipeline_service.py` 中定义：

```text
quantclass > akshare > baostock > postgresql > efinance
```

同日同标的多源价格或量额冲突时，系统仍可发布优先级最高的有效来源，但会把 `publish_status` 标为 `published_with_warning`，`quality_status` 标为 `warning`。

### 3.3 分钟线写入流程

```text
QMT / TDX / AkShare / PostgreSQL legacy
  -> ingest_raw_minute_rows(source=...)
  -> raw_stock_minute_kline_{source}
  -> norm_stock_minute_kline
  -> publish_minute_trade_date()
  -> pub_stock_minute_kline
  -> market_stock_minute_kline
```

分钟线来源优先级：

```text
qmt > tdx > postgresql > akshare
```

分钟线按分钟粒度合成，高优先级缺某一分钟时可由低优先级补齐，发布层记录 `primary_source`、`source_mix`、`coverage_ratio` 和缺失分钟。

### 3.4 状态与统计接口

| 接口 | 用途 | 读取策略 |
| --- | --- | --- |
| `GET /v1/system/data-sources` | 页面数据源注册表 | 来自 `data_source_governance.py`。 |
| `GET /v1/system/market-data-status` | 操作员快速看 active table、publish/quality status、覆盖情况 | 调用 `get_market_data_publish_status()`。 |
| `GET /v1/backtest-data/stats` | 设置页“已下载数据”统计 | 走物理表估算或轻量汇总，避免全量扫描巨大统一视图。 |
| `GET /v1/backtest-data/daily-kline/governance-summary` | 日 K 治理摘要 | 对比 legacy、published、norm、raw、Parquet cache。 |
| `GET /v1/backtest-data/daily-kline/coverage-calendar` | 日 K 年历覆盖 | 查 `stock_daily_kline`/发布层统计后按日期展示。 |

## 4. 模块数据来源与调用机制

### 4.1 控制台、组合、自选和跟踪看板

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/`、`/portfolio`、`/tracking-board` | `/v1/portfolio/overview`、`/v1/dashboard/tracking-board`、`/v1/watchlist`、`/v1/scheduled` | `watchlist_items`、`scheduled_analyses`、`imported_portfolio_positions`、`reports`、实时行情 provider | 自选和定时任务直接读 PostgreSQL；跟踪看板用 `route_to_vendor("get_realtime_quotes")` 拉实时价并合并最新报告。 |
| 持仓导入 | `/v1/portfolio/imports` | 手工录入、截图 VLM、后续也可接仓库来源 | `portfolio_import_service.sync_positions()` 替换同一 source 的快照，可自动创建定时分析。 |
| 持仓截图 | `/v1/portfolio/parse-image` | VLM OpenAI-compatible 或 Anthropic | 图片传后端，`vlm_position_parser` 解析 JSON，再由导入接口入库。 |

主要表：

```text
watchlist_items
scheduled_analyses
imported_portfolio_positions
reports
```

### 4.2 股票市场

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/stock-market` | `/v1/market/overview` | QMT 实时指数报价、本地 `index_daily_kline`、`pub_stock_daily_kline` 或 `stock_daily_kline`、AkShare 板块资金流 | 指数优先 QMT quote，失败用本地指数日线；涨跌榜使用最新物理日线表；板块资金流用异步线程缓存。 |
| K 线 | `/v1/market/kline` | `preferred_daily_kline_table()`、指数表、QMT 当日实时 candle | 先查统一日线表或指数表，再 `_append_live_candle()` 补实时。 |
| 日内线 | `/v1/market/intraday` | QMT bridge 或 xtquant，缓存表 | `fetch_intraday_bars()` 先读当日分钟缓存，缺失或强刷时取 QMT，成功后写 `stock_minute_kline`/`index_minute_kline`。 |
| 实时报价 | `/v1/market/quote` | QMT bridge `/market/quotes` 或 xtquant `get_full_tick` | 2.5 秒快速超时，拿不到则返回 404。 |
| 股票搜索 | `/v1/market/stock-search` | 本地股票名称缓存、AkShare 名称搜索、实时/日线价格补充 | 先查本地 `data/cn_stock_map.json`/缓存，找不到再 AkShare。 |
| 缠论叠加 | `/v1/market/kline/chanlun` | 统一日线表 | 后端基于日 K 计算中枢/分型等 overlay。 |

关键点：市场概览为了速度不直接扫 `market_stock_daily_kline` 大视图，而是优先 `pub_stock_daily_kline`/`stock_daily_kline` 最新物理表。

### 4.3 数据下载、设置页和数据治理中心

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/settings` 数据订阅 | `/v1/backtest-data/configs`、`/configs/{id}/run` | `backtest_data_configs`、`backtest_data_watermarks` | 保存订阅配置；手动或 worker 到点创建 `backtest_data_tasks`。 |
| 批量下载 | `/v1/backtest-data/batch-download`、`/tasks` | QuantClass、AkShare、BaoStock、efinance、QMT、AkShare 指数分钟 | `_process_batch_download()` 根据 `DATA_SOURCE_COMPATIBILITY` 自动校验并执行。 |
| 任务状态 | `/v1/backtest-data/tasks`、`/tasks/{id}` | `backtest_data_tasks` | 前端轮询任务进度，完成后查真实覆盖和 stats。 |
| 数据源列表 | `/v1/backtest-data/data-sources` | `data_source_configs` | 数据源配置表，按 priority 展示。 |
| 下载质量 | `/quality-report`、`/quality-check/{table_name}` | PostgreSQL 表质量检查 | `DataQualityManager` 校验完整性。 |

任务类型与来源兼容：

| task_type | 兼容来源 | 写入位置 |
| --- | --- | --- |
| `daily_kline` | `quantclass`、`akshare`、`baostock`、`tushare`、`eastmoney` | raw/norm/pub，必要时刷新日线 Parquet cache。 |
| `minute_kline` | `qmt`、`akshare` | QMT 历史任务先写 `stock_minute_kline`，再同步 raw/pub；AkShare 直接 raw/pub。 |
| `index_data` | `quantclass`、`akshare`、`baostock`、`tushare`、`eastmoney` | `index_daily_data` 或导入器目标表。 |
| `index_minute_kline` | `qmt`、`akshare` | `index_minute_kline`。 |
| `chip_data` | `quantclass` | 通用导入器。 |
| `financial_data` | `quantclass` | 通用导入器。 |
| `research_reports` | `eastmoney` | 当前标记为待实现。 |

### 4.4 策略管理与回测

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/strategies` | `/v1/strategies`、`/v1/strategies/compile-preview`、`/v1/strategies/packs` | 策略库 `strategies`、内置模板、factor registry | `strategy_platform_repository.py` 读写平台策略，`strategy_dsl_compiler.py` 编译 DSL。 |
| `/backtest` | `/v1/backtests`、`/v1/backtests/{run_id}`、`/v1/backtests/{run_id}/stream` | 策略库、日线 Parquet、PostgreSQL 统一日线表、分钟线确认表 | 创建任务后后台执行；状态通过内存事件和数据库持久化；结果明细写 artifact JSON/Parquet。 |
| 回测详情 | `/metrics`、`/equity`、`/trades`、`/signals`、`/positions`、`/orders`、`/watchlists`、`/minute-confirmations` | `backtest_jobs.result`、artifact 目录 | `_artifact_detail_response()` 优先读 artifact Parquet，缺失时读 JSON。 |
| 因子进化 | `/v1/evolution/...` | `strategy_evolution_experiments`、`strategy_evolution_candidates` | 基于现有策略和候选结果生成/接受候选。 |
| 模拟账户 | `/v1/paper/accounts` | `paper_accounts`、`paper_orders` | 策略平台模拟执行，不触碰实盘。 |

回测行情读取顺序：

```text
日线 Parquet cache
  -> PostgreSQL preferred_daily_kline_table()
  -> 写回 Parquet cache
  -> 若仍为空，部分测试/兜底路径生成 synthetic 数据并标记
```

artifact 目录记录格式：

```text
results/strategy_platform/{run_id}/metrics.json
results/strategy_platform/{run_id}/equity.parquet 或 equity.json
results/strategy_platform/{run_id}/trades.parquet 或 trades.json
...
```

### 4.5 实时监控

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/realtime` | `/v1/realtime/monitors` | 策略库 `realtime_monitors`、策略 DSL、QMT 账号配置 | 创建时编译策略，解析监控股票池，保存实例。 |
| 运行控制 | `/start`、`/pause`、`/stop`、`/resume`、`/run-once`、`/fuse-reset` | 策略库状态 | worker 每 5 秒扫描 running 实例，或手动触发一轮。 |
| 行情和分钟线 | 内部调用 QMT | QMT snapshot、QMT quotes、`capture_today_minute_bars()`、分钟缓存表 | 每轮必须确认账户链路 live，再取 quotes 和分钟线；缺失则写事件或熔断。 |
| 事件流 | `/stream`、`/events` | `realtime_events` | 后端写事件表并推送订阅队列，前端 SSE 展示。 |
| 订单和审批 | `/orders`、`/trades`、`/approvals` | `realtime_events`、`realtime_approvals`、QMT 下单接口 | 自动交易前做风控、同 K 线去重、同票多策略冲突审批；实盘默认只读。 |

主要表：

```text
realtime_monitors
realtime_events
realtime_signal_executions
realtime_approvals
```

### 4.6 虚拟仓和实盘仓

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/virtual-warehouse` | `/v1/virtual-warehouse/qmt/overview` | QMT bridge `/snapshot` 或本地 xttrader | 默认可 `prefer_cache`，页面优先展示快照并可后台刷新。 |
| 后台刷新 | `/qmt/refresh`、`/qmt/sync-profiles` | `qmt_sync_profiles`、`qmt_account_snapshots` | profile 控制同步间隔和状态；刷新结果落快照表。 |
| 委托/成交 | `/qmt/orders`、`/qmt/trades` | QMT snapshot 或本地 cache | 读取订单成交快照；下单只允许安全边界内的纸面/模拟链路。 |
| 下单/撤单 | `/qmt/orders`、`/qmt/orders/{id}/cancel` | bridge `/orders`、`/orders/{id}/cancel` 或 xttrader | `_ensure_paper_trading_allowed()` 阻止实盘误操作。 |
| 一键卖出 | `/qmt/orders/bulk-sell`、`/stream` | 当前持仓快照和 QMT 下单接口 | 后台线程逐票执行，SSE 返回进度。 |

状态标签：

```text
实时直连 live
后台在线 background_live
快照可用 snapshot_available
未连接 disconnected
```

### 4.7 智能分析、聊天和历史报告

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/analysis` | `/v1/analyze` | 用户请求、持仓导入上下文、job store | dry-run 直接写 job；非 dry-run 入队，由兼容层/后台任务执行。 |
| 聊天式分析 | `/v1/chat/completions` | LLM、TradingAgents graph、dataflow providers、PostgreSQL 日线 fallback | 解析股票和日期，SSE 推送进度；深度链路失败且未输出内容时回退本地轻量分析。 |
| 历史报告 | `/v1/reports`、`/v1/reports/{id}`、`/v1/reports/latest-by-symbols` | `reports` 表 | 分析完成后 `report_service.create_report()` 固化结果、风险、指标和分析轨迹。 |
| 公告 | `/v1/announcements/latest` | 当前静态 fallback | 返回默认公告。 |

深度分析数据源由 `tradingagents/default_config.py` 控制：

```text
core_stock_apis: cn_akshare,cn_baostock,yfinance
technical_indicators: cn_akshare,cn_baostock,yfinance
fundamental_data: cn_akshare,cn_baostock,yfinance
news_data: cn_akshare,cn_baostock,yfinance
realtime_data: cn_akshare
```

`route_to_vendor()` 会按配置顺序、注册表顺序、熔断器状态依次尝试 provider。报告写入 `reports` 后，控制台、每日复盘、跟踪看板都会复用这些结果。

### 4.8 资讯之眼

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/news-eye` | `/v1/news-eye/items` | `market_news_items`、`market_news_item_symbols` | 页面只读本地资讯缓存，不直接请求外部站。 |
| 手动刷新 | `/v1/news-eye/refresh` | AkShare 函数：`stock_info_global_cls`、`stock_info_global_em`、`stock_info_cjzc_em`、`stock_news_em` | 后端抓取后去重、打情绪、关联板块和股票，再入库。 |
| 单条解读 | `/v1/news-eye/analyze` | 用户配置的新闻 LLM 或通用 LLM | `create_llm_client()` 调 provider，失败时返回错误或规则结果。 |
| 后台刷新 | `ENABLE_NEWS_EYE_WORKER` | 自选股和定时任务关注标的 | worker 周期扫描关注股票并补新闻缓存。 |

主要表：

```text
market_news_items
market_news_item_symbols
```

### 4.9 每日复盘

| 前端入口 | 后端接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| `/daily-review` | `/v1/daily-reviews`、`/history` | `daily_reviews` | 读取当日或最近复盘。 |
| 生成复盘 | `/v1/daily-reviews/generate` | 市场概览 helper、日线宽度统计、资讯缓存、持仓导入、自选、当日报告、LLM | 先生成规则版 JSON，再用 LLM 增强；最终写 `daily_reviews.raw_result_data`。 |
| 配置 | `/v1/daily-reviews/config` | `user_daily_review_configs` | 到点 worker 按用户配置生成。 |
| 推送 | 内部调用 | 企业微信 webhook、SMTP | `push_enabled` 控制 scheduled 生成后是否推送。 |

### 4.10 配置、登录、Token、反馈和调试

| 模块 | 接口 | 数据来源 | 调用机制 |
| --- | --- | --- | --- |
| 登录 | `/v1/auth/request-code`、`/v1/auth/verify-code`、`/v1/auth/me` | `users`、`email_verification_codes`、SMTP | 验证码 hash 入库，验证后发 JWT。 |
| 运行配置 | `/v1/config`、`/v1/config/warmup`、`/v1/config/wecom/warmup` | `user_llm_configs`、环境变量默认值 | 保存 LLM、QMT、企业微信配置，敏感值加密存储。 |
| 用户 Token | `/v1/tokens` | `user_tokens` | token 创建后只展示一次，数据库存 HMAC hash 和 hint。 |
| 反馈 | `/v1/feedbacks` | `feedbacks` | 用户反馈入库，支持未读计数。 |
| 日志调试 | `/v1/debug/log-sources`、`/v1/debug/logs`、`/v1/debug/logs/stream` | 后端日志、任务日志、runtime log 文件 | 分页读日志或 SSE 追踪。 |
| 健康检查 | `/healthz` | 进程状态 | 返回后端健康状态。 |

## 5. 前端页面到接口索引

| 页面 | 主要 API |
| --- | --- |
| 控制台 `/` | `/v1/portfolio/overview`、`/v1/reports/latest-by-symbols`、`/v1/dashboard/tracking-board` |
| 资讯之眼 `/news-eye` | `/v1/news-eye/items`、`/v1/news-eye/refresh`、`/v1/news-eye/analyze` |
| 股票市场 `/stock-market` | `/v1/market/overview`、`/v1/market/kline`、`/v1/market/intraday`、`/v1/market/quote`、`/v1/market/stock-search` |
| 智能分析 `/analysis` | `/v1/analyze`、`/v1/chat/completions`、`/v1/jobs/{job_id}`、`/v1/jobs/{job_id}/result` |
| 历史报告 `/reports` | `/v1/reports`、`/v1/reports/{id}`、`/v1/reports/latest-by-symbols` |
| 每日复盘 `/daily-review` | `/v1/daily-reviews`、`/v1/daily-reviews/history`、`/v1/daily-reviews/generate`、`/v1/daily-reviews/config` |
| 自选和定时 `/portfolio` | `/v1/watchlist`、`/v1/scheduled`、`/v1/portfolio/imports`、`/v1/portfolio/parse-image` |
| 策略管理 `/strategies` | `/v1/strategies`、`/v1/strategies/compile-preview`、`/v1/strategies/templates`、`/v1/factors` |
| 策略回测 `/backtest` | `/v1/backtests`、`/v1/backtests/{run_id}`、`/v1/backtests/{run_id}/stream`、`/v1/backtests/{run_id}/metrics` 等详情接口 |
| 实时监控 `/realtime` | `/v1/realtime/monitors`、`/v1/realtime/monitors/{id}/stream`、`/v1/realtime/approvals` |
| 虚拟仓 `/virtual-warehouse` | `/v1/virtual-warehouse/qmt/overview`、`/qmt/refresh`、`/qmt/orders`、`/qmt/trades`、`/qmt/diagnostics` |
| 日志 `/debug/logs` | `/v1/debug/log-sources`、`/v1/debug/logs`、`/v1/debug/logs/stream` |
| 设置 `/settings` | `/v1/config`、`/v1/system/data-sources`、`/v1/backtest-data/configs`、`/v1/backtest-data/stats`、`/v1/backtest-data/daily-kline/governance-summary` |

## 6. 关键风险和待收紧点

1. QuantClass 凭证不应硬编码在业务文件中，后续应迁移到环境变量或用户/系统配置表；文档仅记录配置入口，不记录真实 key。
2. `stock_daily_kline`、`stock_minute_kline` 是 legacy 主库，不应把它们的最大日期直接当作“业务可用数据是否最新”的唯一依据，应以 `market_stock_*` 或 `pub_stock_*` 为准。
3. 设置页统计和状态页不能全量扫描 `market_stock_minute_kline` 这类大视图，应继续使用物理表估算、缓存统计和指定 symbol 精查。
4. QMT bridge、QMT 本地 xtquant、快照缓存、后台同步状态要继续分开展示，避免把“缓存可用”误读为“实时可交易”。
5. 实盘相关接口默认只读，自动交易应继续要求显式确认、风控、幂等、同 K 线去重和事件留痕。
6. TradingAgents provider chain 里 `yfinance`、`alpha_vantage` 更适合国际数据兜底；A 股正式结论优先使用本地 PostgreSQL、QMT、QuantClass/AkShare/BaoStock 等 A 股链路。

## 7. 快速排查路径

| 问题 | 先查 |
| --- | --- |
| 页面没数据 | 前端接口地址 `getBaseUrl()`，后端端口 `8500`，接口响应，鉴权 token。 |
| 日 K 不新 | `/v1/system/market-data-status`，再比对 `pub_stock_daily_kline`、`market_stock_daily_kline`、`stock_daily_kline`。 |
| 分钟线任务显示完成但没数据 | `backtest_data_tasks` 状态 + `stock_minute_kline`/`pub_stock_minute_kline` symbol 数、最早/最晚时间。 |
| QMT 仓位空 | `/v1/virtual-warehouse/qmt/diagnostics`、bridge `/health`、`qmt_account_snapshots`、用户 QMT 账号配置。 |
| 实时监控停了 | `ENABLE_REALTIME_MONITOR_WORKER`、`realtime_monitor.runtime.log`、`realtime_events`、QMT live 状态。 |
| 资讯为空 | `ENABLE_NEWS_EYE_WORKER`、`market_news_items`、AkShare 资讯函数是否异常。 |
| 回测为空或 synthetic | 回测 `summary.data_source`、`diagnostics.fallback_mode`、Parquet cache、`preferred_daily_kline_table()` 实际返回。 |
| 每日复盘不生成 | `ENABLE_DAILY_REVIEW_WORKER`、`user_daily_review_configs`、`daily_reviews.status`、LLM 配置和规则 fallback。 |
