# 多源行情数据增量治理机制说明

## 1. 当前模式一句话

当前项目采用 **“历史主库只读 + 多源增量治理 + 统一读取视图”** 的模式。

也就是说：

- 已经存在的大规模日 K、1 分钟 K 数据继续留在原来的历史表里，不全量搬家、不复制、不回填。
- 新下载、新同步、新抓取的数据进入多源增量链路，经过 raw、norm、pub 三层治理后发布。
- 业务查询不直接关心旧表还是增量表，而是通过统一视图读取完整结果。

这套机制的目标是：**保留已有真实历史数据，同时让后续新增数据具备来源标记、质量状态、对账记录和可追溯性。**

## 2. 为什么还有“旧表”和“新表”

这里的“旧表/新表”不是数据好坏之分，而是职责不同。

### 旧表：历史主库

旧表包括：

- `stock_daily_kline`
- `stock_minute_kline`

它们存放项目已经积累的大量真实历史行情数据。尤其 `stock_minute_kline` 体量很大，已有约 2.5 亿条 1 分钟 K 线记录。

保留旧表的原因：

- 数据已经真实存在，没必要为了换结构全量重写。
- 1 分钟数据体量太大，全量复制会造成数据库膨胀、索引压力和长时间锁表风险。
- 老表已经被回测、行情、策略等模块长期使用，直接替换风险高。
- 当前需求是“增量治理”，不是历史数据迁移工程。

因此旧表在新机制里被视为 **legacy historical source**：历史真实数据源，默认只读。

### 新表：多源增量治理链路

新表分三层：

- `raw_stock_*`
- `norm_stock_*`
- `pub_stock_*`

它们只承接后续新增数据，负责多源接入、标准化、对账和发布。

新表存在的原因：

- 每个来源的数据先独立落地，方便排查“哪个源给了什么”。
- 不同来源字段格式不同，需要统一标准。
- 多个来源可能同一天、同一只股票给出不同价格或成交量，需要记录冲突。
- 发布给业务前要打上 `source`、`quality_status`、`publish_status`、`coverage_ratio` 等治理字段。

## 3. 表结构职责

### 历史主表

| 表名 | 职责 | 默认写入策略 |
| --- | --- | --- |
| `stock_daily_kline` | 已有日 K 历史主库 | 默认不由增量链路写入 |
| `stock_minute_kline` | 已有 1 分钟 K 历史主库 | 默认不由增量链路写入 |

### Raw 层：来源原始落地点

| 表名示例 | 职责 |
| --- | --- |
| `raw_stock_daily_kline_quantclass` | 量化课堂日 K 原始数据 |
| `raw_stock_daily_kline_akshare` | AkShare 日 K 原始数据 |
| `raw_stock_daily_kline_baostock` | Baostock 日 K 原始数据 |
| `raw_stock_daily_kline_efinance` | efinance 日 K 原始数据 |
| `raw_stock_minute_kline_qmt` | QMT 分钟线原始数据 |
| `raw_stock_minute_kline_tdx` | TDX 分钟线原始数据 |
| `raw_stock_minute_kline_akshare` | AkShare 分钟线原始数据 |

Raw 层原则：**保留来源口径，不直接给业务使用。**

### Norm 层：统一格式层

| 表名 | 职责 |
| --- | --- |
| `norm_stock_daily_kline` | 所有日 K 来源统一到同一字段结构 |
| `norm_stock_minute_kline` | 所有分钟线来源统一到同一字段结构 |

Norm 层原则：**字段统一，仍按来源保留多份候选数据。**

### Pub 层：发布层

| 表名 | 职责 |
| --- | --- |
| `pub_stock_daily_kline` | 日 K 最终发布结果 |
| `pub_stock_minute_kline` | 分钟线最终发布结果 |

Pub 层原则：**这是后续增量数据通过对账后给业务看的结果。**

发布层会带治理字段：

- `source` / `primary_source`：最终采用的主来源
- `source_mix`：分钟线合成时参与的来源集合
- `quality_status`：质量状态，例如 `validated`、`warning`、`partial`
- `publish_status`：发布状态，例如 `published`、`published_with_warning`、`raw_ingested`
- `coverage_ratio`：覆盖率，分钟线用于表示实际 bar 数 / 预期 bar 数
- `source_summary`：来源对账摘要

### Reconciliation 层：对账记录

| 表名 | 职责 |
| --- | --- |
| `daily_kline_reconciliation_runs` | 日 K 每次对账运行记录 |
| `daily_kline_reconciliation_items` | 日 K 每个标的的对账明细 |
| `minute_kline_reconciliation_runs` | 分钟线每次发布运行记录 |
| `minute_kline_reconciliation_items` | 分钟线每个标的的覆盖率、缺口和来源明细 |

这层用于解释“为什么选了这个来源”“为什么有 warning”“缺了哪些分钟”。

## 4. 统一读取视图

业务侧优先读取统一视图：

- `market_stock_daily_kline`
- `market_stock_minute_kline`

视图的逻辑是：

1. 先看增量发布层 `pub_stock_*` 是否有同一标的、同一日期/时间的数据。
2. 如果有，就用发布层数据。
3. 如果没有，就回落到历史主表 `stock_*`。
4. 对 `000001` 和 `000001.SZ` 这类裸代码/带后缀代码，会按同一逻辑标的处理，避免重复。

所以业务不需要自己判断“旧表还是新表”。它只读：

```text
market_stock_daily_kline
market_stock_minute_kline
```

代码里对应入口是：

- `preferred_daily_kline_table()`
- `preferred_minute_kline_table()`

当前它们会优先返回统一视图。

## 5. 增量写入流程

### 日 K 增量

流程：

```text
外部真实数据源
  -> raw_stock_daily_kline_{source}
  -> norm_stock_daily_kline
  -> reconcile_daily_trade_dates()
  -> pub_stock_daily_kline
  -> market_stock_daily_kline 统一读取
```

当前日 K 来源优先级：

```text
quantclass > akshare > baostock > postgresql > efinance
```

如果多个来源同一日数据冲突，发布层仍会选择优先级最高的有效来源，但会标记：

```text
publish_status = published_with_warning
quality_status = warning
```

### 1 分钟 K 增量

流程：

```text
QMT / TDX / AkShare 等真实来源
  -> raw_stock_minute_kline_{source}
  -> norm_stock_minute_kline
  -> publish_minute_trade_date()
  -> pub_stock_minute_kline
  -> market_stock_minute_kline 统一读取
```

当前分钟线来源优先级：

```text
qmt > tdx > postgresql > akshare
```

分钟线会按分钟粒度合成：

- 优先使用更高优先级来源。
- 高优先级来源缺某一分钟时，可以由低优先级来源补齐。
- 记录 `source_mix`。
- 计算 `coverage_ratio`。
- 记录缺失分钟到 `minute_kline_reconciliation_items.missing_times`。

## 6. 旧表默认不会被增量链路写入

当前默认策略：

```text
MARKET_DATA_WRITE_LEGACY_TABLES=0
```

含义：

- 增量下载、QMT 同步、分钟线发布，默认不会写回 `stock_daily_kline` / `stock_minute_kline`。
- 新数据只进入 raw/norm/pub。
- 业务通过 `market_stock_*` 视图看到“历史旧表 + 新增发布层”的合并结果。

只有显式设置：

```text
MARKET_DATA_WRITE_LEGACY_TABLES=1
```

才允许发布层同步回写旧表。当前不建议开启。

注意：仓库里仍有部分历史导入脚本原本就是面向旧主表设计的，例如全市场分钟线历史导入脚本。如果人工运行这些旧脚本，它们可能仍会直接写 `stock_minute_kline`。这属于历史批量导入工具，不是当前多源增量治理链路。

## 7. 数据状态接口

系统接口：

```text
GET /v1/system/data-sources
GET /v1/system/market-data-status
```

示例：

```text
GET /v1/system/market-data-status?symbols=000001.SZ&limit=5
```

返回会区分：

- `tables.daily` / `tables.minute`：业务实际读取的统一视图
- `published_tables.daily` / `published_tables.minute`：增量发布层表
- `publish_status=legacy`：来自历史主表
- `publish_status=published`：来自增量发布层
- `publish_status=published_with_warning`：已发布但存在来源冲突或覆盖率问题

未指定 `symbols` 查询分钟线状态时，为避免扫描 2.5 亿行分钟线，接口只做抽样展示，并返回：

```text
sampled = true
```

需要精确查看某只股票时，应传入 `symbols`。

设置页“已下载数据”使用：

```text
GET /v1/backtest-data/stats
```

这个接口和业务读取不同：它不会对 `market_stock_*` 统一视图做全量 `COUNT`。原因是 `market_stock_minute_kline` 合并了约 2.5 亿条历史分钟线和增量发布层，如果直接扫视图会拖慢设置页。

当前实现是：

- 日 K：优先读 Parquet 回测缓存水位，同时对照 `stock_daily_kline + pub_stock_daily_kline` 的数据库水位，确保增量发布层的最新日期能显示出来。
- 1 分钟 K：从 `stock_minute_kline` 的 PostgreSQL 统计信息快速估算总行数，并合并 `pub_stock_minute_kline` 的增量行数。
- 业务查询仍然走 `market_stock_daily_kline` / `market_stock_minute_kline`，设置页统计只负责轻量展示“已有数据范围”。

## 8. 为什么不是直接把旧表升级成新表

直接改旧表的问题：

- 旧表没有完整来源治理字段。
- 旧表数据量大，直接 ALTER / UPDATE / 回填治理字段成本高。
- 历史数据来源未必能完整还原，强行标记会制造“假追溯”。
- 旧业务依赖多，直接替换风险大。

当前机制选择旁路增量，是为了避免伪造历史来源：

- 旧数据明确标记为 `legacy`。
- 新数据才记录真实来源、真实对账、真实质量状态。
- 两类数据通过视图合并，对业务可用，对治理透明。

## 9. 当前状态判断

如果状态接口看到：

```text
publish_status = legacy
source = postgresql
```

表示当前数据来自已有历史主库。

如果后续新增同步后看到：

```text
publish_status = published
primary_source = qmt
source_mix = qmt
```

表示数据来自新的多源增量发布链路。

如果看到：

```text
publish_status = published_with_warning
```

表示数据已发布，但有冲突、缺口或覆盖率问题，需要结合 reconciliation 表排查。

## 10. 运维建议

- 不要全量复制 `stock_minute_kline` 到 `pub_stock_minute_kline`。
- 不要为了补治理字段反向伪造旧数据来源。
- 新增数据源时，先加 raw 表映射和来源注册，再接入 norm/pub。
- 排查数据问题时，先看 `/v1/system/market-data-status`，再查 reconciliation 明细。
- 需要补历史数据时，应明确是“历史主库补数据”还是“增量治理链路补数据”，不要混用。

## 11. 当前已落地水位

截至 2026-05-06 本次修复后：

- `stock_daily_kline` 保持不变：17,612,672 行，最大日期仍是 2026-04-30。
- `stock_minute_kline` 保持不变：设置页通过估算可见约 250,247,469 行，日期范围 2020-01-02 至 2026-04-30。
- `pub_stock_daily_kline` 已通过量化课堂真实文件增量发布 2026-05-06 的 5,489 条日 K。
- 日 K Parquet 回测缓存已刷新到 2026-05-06。
- `GET /v1/backtest-data/stats` 现在会返回 `daily_kline`、`minute_kline`、`index_minute_kline`，设置页不会再只显示日 K。
