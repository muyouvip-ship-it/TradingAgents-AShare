# TradingAgents-AShare AI 重构执行清单

> 目标：把整个项目拆成 AI 可逐条执行的重构任务。
>
> 执行方式：
> 1. 一次只执行 1 条任务。
> 2. 每完成 1 条，立即把对应任务改成 `- [x]`。
> 3. 只在当前任务相关文件内改动，避免连锁修改。
> 4. 每条任务完成后必须补充：变更文件、变更摘要、验证结果、风险说明。
> 5. 如果发现任务依赖未完成，必须先回退到上游任务。
>
> 重构目标：
> - 保持现有 API 兼容
> - 保持前端可用
> - 保持功能可回滚
> - 让代码结构更清晰、可测、可扩展

## 状态标记

- `- [ ]` 待执行
- `- [~]` 进行中 / 部分完成
- `- [x]` 已完成

## 每条任务的完成格式

建议 AI 在完成后追加如下记录：

```text
完成时间：2026-04-20
任务：T01 - 拆分 `api/main.py` 的职责边界
结果：
- ...
变更文件：
- ...
验证：
- ...
风险：
- ...
```

---

## 任务总览

### P0 - 必须先做
这些任务优先级最高，建议严格按顺序执行。

- [x] **T01** 拆分 `api/main.py` 的职责边界
- [x] **T02** 把 FastAPI app 创建迁移到 `api/app.py`
- [x] **T03** 把生命周期逻辑迁移到 `api/lifespan.py`
- [ ] **T04** 把通用工具函数迁移到 `api/core/` 或 `api/utils/`
- [ ] **T05** 保持所有对外 API 路径不变，确保前端与客户端兼容
- [ ] **T06** 将 `api/main.py` 中业务路由按领域拆分到 `api/routes/`
- [ ] **T07** 将路由中的业务逻辑抽离到 `api/services/`
- [ ] **T08** 将分散的 Pydantic 模型集中到 `api/schemas/`
- [ ] **T09** 新建统一配置入口 `api/core/settings.py`
- [ ] **T10** 删除所有硬编码路径、端口和超时常量
- [ ] **T11** 统一敏感字段加密、脱敏与日志策略
- [ ] **T12** 规范分析任务编排，拆出 orchestration 模块

### P1 - 稳定性与数据治理
- [ ] **T13** 规范 AgentProgressTracker 的状态迁移规则
- [ ] **T14** 为长任务增加阶段级超时与错误分类
- [ ] **T15** 将内存 job store 改为可插拔实现
- [ ] **T16** 统一数据库初始化与 Session 获取方式
- [ ] **T17** 去掉策略数据库中的绝对路径
- [ ] **T18** 统一报告结果结构与字段含义
- [ ] **T19** 审查并优化全局缓存与 TTL 策略

### P2 - 安全与权限
- [ ] **T20** 统一 JWT 与 API Token 验证流程
- [ ] **T21** 为策略管理与敏感配置接口补齐权限控制
- [ ] **T22** 为删除/修改类接口增加审计记录
- [ ] **T23** 避免日志输出 token、key、webhook 等敏感信息

### P3 - 前端重构
- [ ] **T24** 封装统一前端 API client
- [ ] **T25** 整理 Zustand 状态边界
- [ ] **T26** 抽取通用 UI 组件并减少重复逻辑
- [ ] **T27** 优化大列表、图表、SSE 的渲染性能
- [ ] **T28** 统一前端错误、加载、空状态体验

### P4 - 测试与交付
- [ ] **T29** 为核心工具函数补单元测试
- [ ] **T30** 为关键 API 补集成测试
- [ ] **T31** 为 SSE/流式链路补测试
- [ ] **T32** 为前端核心交互补测试
- [ ] **T33** 将 lint、test、typecheck 纳入 CI 门禁
- [ ] **T34** 更新运行文档、Docker 文档与 API 示例
- [ ] **T35** 输出重构后的架构说明与迁移说明

---

## 详细任务卡

### T01 - 拆分 `api/main.py` 的职责边界
**目标**
- 将超大入口文件按职责拆分，为后续所有重构打基础。

**范围**
- 仅整理结构，不改业务逻辑。

**输入**
- 当前 `api/main.py`

**输出**
- 拆分方案和新的文件职责边界

**验收标准**
- `api/main.py` 不再承担所有职责
- 应用初始化、路由注册、生命周期、工具函数边界清晰
- 对外 API 行为保持不变

**依赖**
- 无

**完成记录**
- 完成时间：2026-04-20
- 任务：T01 - 拆分 `api/main.py` 的职责边界
- 结果：
  - 已将应用创建、生命周期、日志、版本、HTTP 工具、股票映射、配置构建与部分业务路由逐步拆分到 `api/app.py`、`api/lifespan.py`、`api/core/`、`api/routes/`、`api/services/`、`api/schemas/`。
  - `api/main.py` 已从“单体入口”转为“过渡兼容入口”，职责边界已明显收敛。
- 变更文件：
  - `api/app.py`
  - `api/lifespan.py`
  - `api/core/__init__.py`
  - `api/core/logging.py`
  - `api/core/http_utils.py`
  - `api/core/versioning.py`
  - `api/core/stock_map.py`
  - `api/core/stock_utils.py`
  - `api/core/datetime_utils.py`
  - `api/core/runtime_config.py`
  - `api/deps.py`
  - `api/schemas/auth.py`
  - `api/schemas/config.py`
  - `api/routes/health.py`
  - `api/routes/auth.py`
  - `api/routes/config.py`
  - `api/services/config_service.py`
  - `api/main.py`
- 验证：
  - 已对相关文件执行 lint 检查，当前无新增 lint 错误。
- 风险：
  - 当前仍处于分步迁移阶段，若后续还有旧引用残留，需要继续收口并逐步验证路由挂载与导入链路。

### T02 - 把 FastAPI app 创建迁移到 `api/app.py`
**目标**
- 让应用创建逻辑独立，避免与业务代码混在一起。

**输入**
- `api/main.py`

**输出**
- `api/app.py`

**验收标准**
- FastAPI app 在单独模块中创建
- 中间件、docs 配置、router 注入方式可追踪
- 启动入口仍可正常工作

**依赖**
- T01

**完成记录**
- 完成时间：2026-04-20
- 任务：T02 - 把 FastAPI app 创建迁移到 `api/app.py`
- 结果：
  - 已将 FastAPI 应用创建、版本读取、生产环境文档开关与 CORS 中间件配置迁移到 `api/app.py`。
  - `api/app.py` 现在承担应用创建职责，入口更清晰。
- 变更文件：
  - `api/app.py`
  - `api/core/http_utils.py`
  - `api/core/versioning.py`
- 验证：
  - 对相关文件执行 lint 检查，当前无新增 lint 错误。
- 风险：
  - 当前项目仍在持续拆分，后续若变更路由注册方式，需要确认 `app` 引用路径一致。

### T03 - 把生命周期逻辑迁移到 `api/lifespan.py`
**目标**
- 把数据库初始化、预加载、清理逻辑抽离出来。

**输入**
- `api/main.py`

**输出**
- `api/lifespan.py`

**验收标准**
- 启动/关闭逻辑集中管理
- 预加载逻辑仍能正常执行
- 应用关闭时资源可以释放

**依赖**
- T01, T02

**完成记录**
- 完成时间：2026-04-20
- 任务：T03 - 把生命周期逻辑迁移到 `api/lifespan.py`
- 结果：
  - 已将数据库初始化、策略库初始化、job store 清理、交易日历预加载、股票映射预加载、退出清理逻辑迁移到 `api/lifespan.py`。
  - `api/main.py` 不再承担生命周期职责。
- 变更文件：
  - `api/lifespan.py`
  - `api/main.py`
- 验证：
  - 对相关文件执行 lint 检查，当前无新增 lint 错误。
- 风险：
  - 生命周期中仍依赖若干全局初始化对象，后续如果改成多进程/分布式部署，需要再做资源初始化隔离。

### T04 - 把通用工具函数迁移到 `api/core/` 或 `api/utils/`
**目标**
- 让通用函数与业务路由分离。

**范围**
- 字符串处理、时间、配置合并、脱敏、SSE 封装、stock map 等工具函数。

**验收标准**
- 相同工具不在多个文件重复定义
- 新模块可被其他路由复用
- 不改变对外行为

**依赖**
- T01

---

### T05 - 保持所有对外 API 路径不变
**目标**
- 确保重构过程不破坏前端和第三方客户端。

**验收标准**
- 所有现有接口路径、请求字段、响应字段兼容
- 关键页面仍能正常访问
- SSE 事件名称保持兼容或提供兼容层

**依赖**
- T01~T04 过程中持续满足

---

### T06 - 将 `api/main.py` 中业务路由按领域拆分到 `api/routes/`
**目标**
- 把路由按领域拆到独立文件，降低单文件复杂度。

**建议拆分**
- `auth`
- `analysis`
- `reports`
- `portfolio`
- `watchlist`
- `scheduled`
- `config`
- `feedback`
- `sponsors`
- `market`
- `backtest`
- `strategies`

**验收标准**
- 路由文件职责单一
- `include_router` 集中注册
- OpenAPI 更清晰

**依赖**
- T01, T02

---

### T07 - 将路由中的业务逻辑抽离到 `api/services/`
**目标**
- 路由只负责收参、鉴权、返回；核心逻辑进入 service 层。

**验收标准**
- 路由层中不再堆复杂业务逻辑
- 业务逻辑具备可单测能力
- 同一业务不会在多个路由里复制实现

**依赖**
- T06

---

### T08 - 将分散的 Pydantic 模型集中到 `api/schemas/`
**目标**
- 统一请求/响应模型，减少重复定义。

**验收标准**
- 模型按领域拆分
- 路由文件中的模型数量明显下降
- 请求/响应类型稳定可复用

**依赖**
- T06

---

### T09 - 新建统一配置入口 `api/core/settings.py`
**目标**
- 用统一配置层管理环境变量、默认值和运行时设置。

**验收标准**
- 配置读取集中化
- 开发/测试/生产配置可区分
- 配置优先级明确

**依赖**
- T04

---

### T10 - 删除所有硬编码路径、端口和超时常量
**目标**
- 提升跨环境部署能力。

**重点检查**
- 数据库文件路径
- 前端代理地址
- 后端端口
- job timeout
- warmup timeout
- probe timeout

**验收标准**
- 没有与个人机器绑定的绝对路径
- 所有部署敏感值来自配置

**依赖**
- T09

---

### T11 - 统一敏感字段加密、脱敏与日志策略
**目标**
- 防止 API Key、Webhook、Token 等敏感信息泄露。

**验收标准**
- 日志中不直接打印敏感信息
- 敏感字段展示有统一 mask 规则
- 配置保存与读取符合安全要求

**依赖**
- T09, T10

---

### T12 - 规范分析任务编排，拆出 orchestration 模块
**目标**
- 将多智能体分析流程抽成独立编排层。

**验收标准**
- 数据采集、推理、抽取、落库拆开
- 任务输入输出契约明确
- SSE 仍可正常发送事件

**依赖**
- T01~T07

---

### T13 - 规范 AgentProgressTracker 的状态迁移规则
**目标**
- 让分析状态更稳定、更可理解。

**验收标准**
- 状态枚举统一
- 迁移规则清晰
- 重复事件被去重
- 异常阶段有兜底状态

**依赖**
- T12

---

### T14 - 为长任务增加阶段级超时与错误分类
**目标**
- 提升任务稳定性和失败可定位性。

**验收标准**
- 分阶段超时可控
- 错误分类明确
- 超时/失败信息可回传前端

**依赖**
- T12

---

### T15 - 将内存 job store 改为可插拔实现
**目标**
- 为多进程和高并发场景做准备。

**验收标准**
- job store 可切换实现
- 内存版与 Redis 版接口一致
- 任务状态不会仅依赖单进程内存

**依赖**
- T12

---

### T16 - 统一数据库初始化与 Session 获取方式
**目标**
- 清理散落的数据库连接创建方式。

**验收标准**
- engine / session 创建集中管理
- 路由不再自己创建数据库连接
- 所有写操作使用短生命周期 session

**依赖**
- T01, T09

---

### T17 - 去掉策略数据库中的绝对路径
**目标**
- 修复跨机器/容器不可用问题。

**验收标准**
- 策略数据库路径来自配置
- 策略 API 可在不同环境运行

**依赖**
- T09, T16

---

### T18 - 统一报告结果结构与字段含义
**目标**
- 降低报告对象的字段歧义。

**验收标准**
- 推理结果、结构化结果、展示字段拆清楚
- 历史报告存储结构稳定
- 报告读取逻辑一致

**依赖**
- T07, T08, T16

---

### T19 - 审查并优化全局缓存与 TTL 策略
**目标**
- 防止缓存失效不一致和内存泄漏。

**验收标准**
- 缓存过期策略明确
- 线程安全问题被处理
- 多进程部署风险有说明

**依赖**
- T04, T09

---

### T20 - 统一 JWT 与 API Token 验证流程
**目标**
- 让认证逻辑更清晰、更容易维护。

**验收标准**
- 登录态与 API Token 解析路径清晰
- 认证失败信息一致
- 统一依赖注入方式

**依赖**
- T01, T06

---

### T21 - 为策略管理与敏感配置接口补齐权限控制
**目标**
- 防止未授权修改高风险数据。

**验收标准**
- 策略 CRUD 有权限控制
- 敏感配置修改有权限控制
- 公共接口与私有接口边界明确

**依赖**
- T20

---

### T22 - 为删除/修改类接口增加审计记录
**目标**
- 让关键操作可追踪、可回放。

**验收标准**
- 删除/修改行为有审计信息
- 审计日志结构化
- 可定位操作者和对象

**依赖**
- T20, T21

---

### T23 - 避免日志输出 token、key、webhook 等敏感信息
**目标**
- 收紧安全边界。

**验收标准**
- 所有敏感字段输出前脱敏
- 异常日志不泄露秘密
- warmup / probe 日志符合安全要求

**依赖**
- T11, T22

---

### T24 - 封装统一前端 API client
**目标**
- 统一前端网络请求方式。

**验收标准**
- 请求、错误、超时、鉴权统一处理
- SSE 处理统一
- 组件不直接拼请求细节

**依赖**
- 无

---

### T25 - 整理 Zustand 状态边界
**目标**
- 让全局状态和页面状态分开。

**验收标准**
- store 颗粒度合理
- 派生状态不重复存储
- 页面间状态耦合降低

**依赖**
- T24

---

### T26 - 抽取通用 UI 组件并减少重复逻辑
**目标**
- 提高前端复用率和一致性。

**验收标准**
- 通用表格、弹窗、标签、空状态可复用
- 页面代码更短更清晰

**依赖**
- T24

---

### T27 - 优化大列表、图表、SSE 的渲染性能
**目标**
- 提升复杂页面响应速度。

**验收标准**
- 大列表虚拟化或分页
- 图表按需加载
- SSE 更新不会频繁全量重渲染

**依赖**
- T24, T25, T26

---

### T28 - 统一前端错误、加载、空状态体验
**目标**
- 提升用户使用体验。

**验收标准**
- 错误提示统一
- 加载态统一
- 空状态有可操作引导

**依赖**
- T24, T26

---

### T29 - 为核心工具函数补单元测试
**目标**
- 为重构提供基础保障。

**验收标准**
- 关键工具函数覆盖测试
- 核心解析与配置逻辑可回归

**依赖**
- T04, T09, T10

---

### T30 - 为关键 API 补集成测试
**目标**
- 保证关键接口在重构后仍可用。

**验收标准**
- auth/config/market/reports 等核心接口有集成测试
- 关键错误路径也被覆盖

**依赖**
- T05, T06, T07

---

### T31 - 为 SSE/流式链路补测试
**目标**
- 保障长任务与流式事件稳定性。

**验收标准**
- job.created / job.running / job.completed / job.failed 顺序可验证
- 断流与超时可验证

**依赖**
- T12, T14

---

### T32 - 为前端核心交互补测试
**目标**
- 降低 UI 回归风险。

**验收标准**
- 核心页面具备组件测试或 E2E 测试
- 关键交互有回归覆盖

**依赖**
- T24~T28

---

### T33 - 将 lint、test、typecheck 纳入 CI 门禁
**目标**
- 让质量检查自动化。

**验收标准**
- CI 中 lint/test/typecheck 全部执行
- 失败会阻止合并

**依赖**
- T29~T32

---

### T34 - 更新运行文档、Docker 文档与 API 示例
**目标**
- 让重构后的项目更易使用。

**验收标准**
- 启动、部署、配置、调用方式同步更新
- 文档与实际代码一致

**依赖**
- T02~T23

---

### T35 - 输出重构后的架构说明与迁移说明
**目标**
- 给后续维护者提供完整上下文。

**验收标准**
- 目录结构说明清晰
- 关键流程与配置优先级清晰
- 迁移说明可指导旧调用方升级

**依赖**
- T34
