# TradingAgents-AShare AI 重构执行清单

> 目标：把整个项目拆成 AI 可逐条执行的重构任务。
>
> 执行方式：
> 1. 一次只执行 1 条任务。
> 2. 每完成 1 条，立即把对应任务改成 `- [x]`。
> 3. 只在当前任务相关文件内改动，避免连锁修改。
> 4. 每条任务完成后必须补充：变更文件、变更摘要、验证结果、风险说明。
> 5. 如果发现任务依赖未完成，必须先回退到上游任务。
>
> 重构目标：
> - 保持现有 API 兼容
> - 保持前端可用
> - 保持功能可回滚
> - 让代码结构更清晰、可测、可扩展

## 状态标记

- `- [ ]` 待执行
- `- [~]` 进行中 / 部分完成
- `- [x]` 已完成

## 每条任务的完成格式

建议 AI 在完成后追加如下记录：

```text
完成时间：2026-04-20
任务：T01 - 拆分 `api/main.py` 的职责边界
结果：
- ...
变更文件：
- ...
验证：
- ...
风险：
- ...
```

---

## 任务总览

### P0 - 必须先做
这些任务优先级最高，建议严格按顺序执行。

- [x] **T01** 拆分 `api/main.py` 的职责边界
- [x] **T02** 把 FastAPI app 创建迁移到 `api/app.py`
- [x] **T03** 把生命周期逻辑迁移到 `api/lifespan.py`
- [x] **T04** 把通用工具函数迁移到 `api/core/` 或 `api/utils/`
- [x] **T05** 保持所有对外 API 路径不变，确保前端与客户端兼容
- [x] **T06** 将 `api/main.py` 中业务路由按领域拆分到 `api/routes/`
- [x] **T07** 将路由中的业务逻辑抽离到 `api/services/`
- [x] **T08** 将分散的 Pydantic 模型集中到 `api/schemas/`
- [x] **T09** 新建统一配置入口 `api/core/settings.py`
- [x] **T10** 删除所有硬编码路径、端口和超时常量
- [x] **T11** 统一敏感字段加密、脱敏与日志策略
- [x] **T12** 规范分析任务编排，拆出 orchestration 模块

### P1 - 稳定性与数据治理
- [x] **T13** 规范 AgentProgressTracker 的状态迁移规则
- [x] **T14** 为长任务增加阶段级超时与错误分类
- [x] **T15** 将内存 job store 改为可插拔实现
- [x] **T16** 统一数据库初始化与 Session 获取方式
- [x] **T17** 去掉策略数据库中的绝对路径
- [x] **T18** 统一报告结果结构与字段含义
- [x] **T19** 审查并优化全局缓存与 TTL 策略

### P2 - 安全与权限
- [x] **T20** 统一 JWT 与 API Token 验证流程
- [x] **T21** 为策略管理与敏感配置接口补齐权限控制
- [x] **T22** 为删除/修改类接口增加审计记录
- [x] **T23** 避免日志输出 token、key、webhook 等敏感信息

### P3 - 前端重构
- [x] **T24** 封装统一前端 API client
- [x] **T25** 整理 Zustand 状态边界
- [x] **T26** 抽取通用 UI 组件并减少重复逻辑
- [x] **T27** 优化大列表、图表、SSE 的渲染性能
- [x] **T28** 统一前端错误、加载、空状态体验

### P4 - 测试与交付
- [x] **T29** 为核心工具函数补单元测试
- [x] **T30** 为关键 API 补集成测试
- [x] **T31** 为 SSE/流式链路补测试
- [x] **T32** 为前端核心交互补测试
- [x] **T33** 将 lint、test、typecheck 纳入 CI 门禁
- [x] **T34** 更新运行文档、Docker 文档与 API 示例
- [x] **T35** 输出重构后的架构说明与迁移说明
