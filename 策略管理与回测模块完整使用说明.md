# 策略管理与回测模块完整使用说明

更新时间：2026-04-21

## 1. 使用目标

本说明面向“完整状态”下的策略管理与策略回测模块，覆盖从策略创建、模板选择、DSL 编译、回测、结果查看、进化实验到纸交易验证的完整使用流程。

完整状态下，本模块应支持：

- 策略管理：创建、查看、克隆、版本管理、激活/暂停。
- AI 创建策略：白话输入生成结构化 DSL。
- 策略模板：快速选择常见策略框架并调参数。
- 策略 DSL：可解释、可校验、可编译。
- 回测工作台：日线/分钟线、多周期波段、A 股撮合、明细分页。
- 进化实验：基于 Trade Snapshot 归因和样本外验证生成候选策略。
- 纸交易：把策略信号落到模拟账户，验证准实盘效果。

## 2. 启动项目

### 2.1 启动后端

在项目根目录执行：

```bash
uvicorn api.app:app --host 127.0.0.1 --port 8000 --reload
```

后端地址：

```text
http://127.0.0.1:8000
```

常用探活接口：

```bash
curl http://127.0.0.1:8000/v1/strategies/templates
```

### 2.2 启动前端

在 `frontend` 目录执行：

```bash
npm run dev -- --host 127.0.0.1
```

前端默认地址：

```text
http://127.0.0.1:5174/
```

如果端口被占用，Vite 会自动切到下一个端口，例如：

```text
http://127.0.0.1:5175/
```

## 3. 页面入口

| 功能 | 路由 | 说明 |
|---|---|---|
| 策略管理中心 | `/strategies` | 查看策略、版本、绩效、进化、纸交易入口 |
| AI 策略创建器 | `/strategies/create` | 白话创建策略、模板参数化、DSL 预览 |
| 回测工作台 | `/backtest` | 发起回测、查看资金曲线和明细 |

## 4. 推荐完整工作流

```text
选择模板 / 白话描述
        ↓
生成 DSL 草案
        ↓
查看待确认项、数据依赖、风险提示
        ↓
保存策略草稿
        ↓
编译 DSL
        ↓
发起回测
        ↓
查看资金曲线、交易、订单、候选池、分钟确认、快照
        ↓
创建进化实验
        ↓
查看候选策略并接受
        ↓
生成新版本
        ↓
再次回测
        ↓
进入纸交易账户验证
```

## 5. 策略创建

### 5.1 三种创建方式总览

进入：

```text
/strategies/create
```

当前创建页支持三种入口：

| 创建方式 | 适用场景 | 作用 |
|---|---|---|
| 模板创建 | 想快速起一个稳定骨架 | 加载预置 DSL 骨架、默认参数、默认策略说明 |
| 白话生成 | 想从业务语言快速生成初稿 | 通过 LLM 生成结构化 DSL 草案 |
| DSL 直编 | 已明确策略结构或要精确控制 | 直接编写/粘贴 DSL JSON，保存后立即编译 |

其中“策略模板”不是替代 LLM，而是：

- 给你一个可直接运行的 DSL 起点；
- 帮你预填常见策略结构、参数和默认提示词；
- 适合先模板起步，再切换到白话或 DSL 继续细化。

### 5.2 使用策略模板创建

进入：

```text
/strategies/create
```

第一步选择策略模板。

完整状态下建议内置这些模板：

| 模板 | 场景 | 说明 |
|---|---|---|
| 算力业绩高增波段 | 选股 + 波段交易 | 算力概念、中盘成长、多周期波段 |
| 鳄鱼张口趋势突破 | 趋势突破 | 强趋势、Alligator 张口、分钟确认 |
| 低波轮动防守 | 低波组合 | 高股息/低波/防守型轮动 |

选择模板后，可以调整参数：

- 选股数量 `top_n`
- 最低评分 `min_score`
- 最大持仓数 `max_positions`
- 单笔风险 `risk_per_trade_pct`
- 目标波动率 `target_volatility_pct`

调整参数后，右侧 DSL 预览会同步变化。

### 5.3 使用白话创建

在白话输入框中输入策略需求，例如：

```text
创建一个选股+波段交易策略：算力板块、资金在100亿到200亿之间、业绩暴增的股票。周线看趋势，日线做波段，分钟线确认入场。
```

点击：

```text
生成 DSL 草案
```

系统应返回：

- 策略名称
- 策略类型
- 意图摘要
- 待确认项
- 数据依赖
- 风险提示
- DSL JSON
- 编译报告
- LLM 运行配置摘要

策略管理与回测相关 LLM 共用“设置”页面里的模型配置：

- `llm_provider`
- `quick_think_llm`
- `deep_think_llm`
- `backend_url`
- `api_key`

接口响应只展示脱敏摘要，例如模型提供商、快速模型、深度模型、是否已有 API Key，不返回明文密钥。

### 5.4 使用 DSL 直接创建

切换到“DSL 直编”模式后，可直接填写：

- 策略名称
- 策略说明
- 策略类型
- DSL JSON

系统行为：

- 保存前先执行 JSON 解析；
- 解析失败时阻止保存，并提示具体格式错误；
- 解析成功后直接保存策略定义并调用编译接口；
- 选股策略会自动清空 `entry` / `exit` 交易条件，确保类型一致。

适合场景：

- 你已经明确 DSL 结构；
- 你想手动控制因子、规则、执行参数；
- 你想绕过 LLM，直接进入“保存 → 编译 → 回测”流程。

### 5.5 待确认项

当用户描述存在歧义时，系统必须展示待确认项。

示例：

| 用户描述 | 默认解释 | 需要确认 |
|---|---|---|
| 资金 100 亿到 200 亿 | 流通市值 | 是否指成交额或主力资金 |
| 业绩暴增 | 净利润同比高增长 | 是否使用营收或扣非净利润 |
| 算力板块 | 概念标签 | 概念数据是否完整 |

### 5.4 保存策略

点击：

```text
保存草稿
```

或：

```text
保存并进入回测
```

保存后策略状态默认为：

```text
draft
```

## 6. 策略管理中心

进入：

```text
/strategies
```

### 6.1 策略列表

策略卡片应展示：

- 策略名称
- 策略类型
- 状态
- 来源：manual / llm / evolution / template
- 当前版本
- 总收益
- 年化收益
- 夏普比率
- 最大回撤
- 胜率
- 最近运行时间

### 6.2 常用操作

| 操作 | 说明 |
|---|---|
| 激活策略 | 将策略状态设为 active |
| 暂停策略 | 将策略状态设为 paused |
| 克隆策略 | 复制当前策略为新草稿 |
| 查看版本历史 | 查看所有 DSL 版本 |
| 发起回测 | 跳转到回测工作台 |
| 发起进化 | 基于最近回测创建进化实验 |
| 运行纸交易 | 在模拟账户中生成订单 |

### 6.3 版本管理

每次修改策略 DSL 后，应生成新版本。

版本包含：

- version
- dsl
- compile_status
- compiled_hash
- change_summary
- created_at

候选策略被接受后，也应生成新版本，不覆盖原策略。

## 7. 策略 DSL 说明

### 7.1 顶层结构

```json
{
  "schema_version": "1.0",
  "strategy_type": "portfolio",
  "universe": {},
  "factor_model": {},
  "entry": {},
  "exit": {},
  "position": {},
  "risk": {},
  "execution": {},
  "evolution": {}
}
```

### 7.2 股票池 `universe`

常用字段：

| 字段 | 说明 |
|---|---|
| market | 市场，A 股为 `A_SHARE` |
| include_concepts | 包含概念 |
| exclude_st | 排除 ST |
| exclude_suspended | 排除停牌 |
| min_listing_days | 最少上市天数 |
| filters | 过滤条件 |

示例：

```json
{
  "market": "A_SHARE",
  "include_concepts": ["算力", "AI算力"],
  "exclude_st": true,
  "exclude_suspended": true,
  "min_listing_days": 250,
  "filters": [
    {
      "field": "float_market_cap",
      "op": "between",
      "value": [10000000000, 20000000000],
      "unit": "CNY"
    }
  ]
}
```

### 7.3 多因子模型 `factor_model`

支持：

- 成长因子
- 质量因子
- 动量因子
- 波动率因子
- 资金流因子
- 量价因子

示例：

```json
{
  "score_method": "weighted_sum",
  "rebalance_frequency": "weekly",
  "factors": [
    {
      "name": "net_profit_growth_yoy",
      "weight": 0.35,
      "direction": "higher_better",
      "transform": "rank_pct"
    },
    {
      "name": "money_flow_strength_20d",
      "weight": 0.25,
      "direction": "higher_better",
      "transform": "rank_pct"
    }
  ],
  "select": {
    "top_n": 30,
    "min_score": 0.65
  }
}
```

### 7.4 入场条件 `entry`

多周期波段推荐：

- 周线看趋势
- 日线看波段信号
- 分钟线确认入场

示例：

```json
{
  "logic": "all",
  "conditions": [
    {
      "type": "trend",
      "timeframe": "1w",
      "field": "close",
      "op": "above",
      "indicator": "ma20"
    },
    {
      "type": "alligator_opening",
      "timeframe": "1d",
      "params": {
        "jaw": 13,
        "teeth": 8,
        "lips": 5
      },
      "direction": "bullish"
    },
    {
      "type": "intraday_confirm",
      "timeframe": "30m",
      "conditions": [
        {
          "type": "cross_above",
          "left": "close",
          "right": "vwap"
        }
      ]
    }
  ]
}
```

### 7.5 出场条件 `exit`

支持：

- 均线跌破
- ATR 移动止损
- 因子排名跌出
- 止损止盈
- 风控熔断

### 7.6 仓位 `position`

支持模型：

| 方法 | 说明 |
|---|---|
| equal_weight | 等权 |
| factor_weight | 因子加权 |
| volatility_target | 波动率目标 |
| risk_budget | ATR 风险预算 |

增强能力：

- 最大单票仓位
- 最大组合仓位
- 现金保留
- 单笔风险预算
- 金字塔加仓
- 分批建仓

### 7.7 风控 `risk`

常用字段：

| 字段 | 说明 |
|---|---|
| stop_loss_pct | 止损 |
| take_profit_pct | 止盈 |
| trailing_stop_pct | 移动止损 |
| max_drawdown_pct | 组合最大回撤熔断 |
| max_daily_loss_pct | 单日亏损熔断 |
| max_positions | 最大持仓数 |
| cooldown_days_after_stop | 止损后冷却期 |

### 7.8 执行 `execution`

A 股默认规则：

- T+1
- 100 股整数手
- 涨停不可买
- 跌停不可卖
- 停牌不可成交
- 佣金
- 印花税
- 滑点
- 成交量容量限制

## 8. 编译策略

策略保存后，应进行 DSL 编译。

接口：

```text
POST /v1/strategies/{strategy_id}/compile
```

编译结果包括：

- status
- errors
- warnings
- required_fields
- compiled_targets
- execution_plan
- timeframes_required
- minute_requirements
- backend_resolution
- pending_confirmations
- future_function_risks

如果编译失败，必须先修改 DSL。

常见失败原因：

- 非法字段
- 非法枚举值
- 未来函数
- 不支持的操作符
- 必填块缺失

## 9. 回测工作台

进入：

```text
/backtest
```

### 9.1 回测配置

常用参数：

| 参数 | 说明 |
|---|---|
| 策略 | 选择策略 |
| 策略版本 | 选择版本 |
| 股票池 | 指定代码列表，留空可走默认股票池 |
| 开始日期 | 回测开始 |
| 结束日期 | 回测结束 |
| 初始资金 | 默认 1,000,000 |
| 周期 | daily / daily_minute |
| 基准 | 如沪深300 |
| 分钟确认 | 是否开启分钟线确认 |

### 9.2 回测模式

#### daily

只使用日线/周线。

适合：

- 全市场粗筛
- 长周期回测
- 快速参数比较

#### daily_minute

先使用日线生成 Watchlist，再对候选股票读取分钟线确认。

适合：

- 波段交易
- 入场点优化
- 多周期策略验证

### 9.3 Walk-forward 回测

完整状态下，回测请求可携带：

```json
{
  "walk_forward": {
    "enabled": true,
    "train_days": 252,
    "test_days": 63,
    "step_days": 63
  }
}
```

系统应输出：

- window_count
- 每个窗口训练区间
- 每个窗口测试区间
- 训练指标
- 测试指标
- stitched final capital

## 10. 回测结果说明

### 10.1 指标摘要

核心指标：

- 总收益率
- 年化收益
- 夏普比率
- 最大回撤
- 胜率
- 盈亏比
- 波动率
- 最终资金
- Calmar

### 10.2 图表

完整状态下应展示：

- 资金曲线
- 回撤曲线
- 收益分布
- 月度收益
- Walk-forward 分段表现

### 10.3 明细表

应支持筛选、排序、分页、导出。

| 明细 | 接口 |
|---|---|
| 资金曲线 | `GET /v1/backtests/{id}/equity` |
| 交易记录 | `GET /v1/backtests/{id}/trades` |
| 持仓历史 | `GET /v1/backtests/{id}/positions` |
| 信号明细 | `GET /v1/backtests/{id}/signals` |
| 订单生命周期 | `GET /v1/backtests/{id}/orders` |
| 候选池 | `GET /v1/backtests/{id}/watchlists` |
| 分钟确认 | `GET /v1/backtests/{id}/minute-confirmations` |
| 交易快照 | `GET /v1/backtests/{id}/trade-snapshots` |

### 10.4 诊断信息

完整状态下应展示：

- engine_mode
- feature_engine
- scan_engine
- fallback_mode
- minute_aggregation
- watchlist_days
- minute_load_count
- minute_symbol_days
- confirm_hit_rate
- minute_data_missing
- order_count
- risk_event_count
- backend_resolution

如果看到：

```text
fallback_mode = true
```

说明当前结果可用于流程验证，但性能和精度应以真引擎环境为准。

## 11. 回测 Artifact

每次回测应写入：

```text
data/artifacts/backtests/{run_id}/
```

典型文件：

```text
compiled_strategy.json
metrics.json
engine_diagnostics.json
equity.json / equity.parquet
trades.json / trades.parquet
trade_snapshots.json / trade_snapshots.parquet
orders.json / orders.parquet
watchlists.json / watchlists.parquet
minute_confirmations.json / minute_confirmations.parquet
signals.json / signals.parquet
positions.json / positions.parquet
```

其中：

```text
trade_snapshots
```

是策略自进化的关键输入。

## 12. 策略进化

### 12.1 创建进化实验

接口：

```text
POST /v1/evolution/experiments
```

输入：

```json
{
  "strategy_id": "xxx",
  "objective": "calmar_then_win_rate",
  "search_space": {}
}
```

系统应读取最近一次完成的回测和 Trade Snapshot。

### 12.2 候选策略

候选策略应包含：

- id
- name
- score
- metrics
- improvement_summary
- risk_flags
- dsl_patch

候选策略不能自动覆盖原策略。

### 12.3 接受候选

接口：

```text
POST /v1/evolution/candidates/{candidate_id}/accept
```

接受后：

1. 复制基础策略。
2. 应用 `dsl_patch`。
3. 生成新策略版本。
4. 保留原策略。
5. 可继续回测或进入纸交易。

## 13. 纸交易

### 13.1 创建纸交易账户

接口：

```text
POST /v1/paper/accounts
```

示例：

```json
{
  "id": "demo",
  "name": "测试纸交易账户",
  "initial_capital": 1000000
}
```

### 13.2 运行策略

接口：

```text
POST /v1/paper/accounts/{account_id}/run-strategy?strategy_id={strategy_id}
```

完整状态下，纸交易应优先使用：

1. 最近回测中的可执行订单。
2. 最近回测中的买入信号。
3. 默认 fallback 信号。

### 13.3 查看纸交易结果

| 内容 | 接口 |
|---|---|
| 订单 | `GET /v1/paper/accounts/{id}/orders` |
| 持仓 | `GET /v1/paper/accounts/{id}/positions` |
| 权益 | `GET /v1/paper/accounts/{id}/equity` |

## 14. 数据策略

长期保留：

- 日 K
- 1 分钟 K

不长期保留：

- 5m
- 15m
- 30m
- 周线

生成方式：

- 周线：由日 K 聚合。
- 5m/15m/30m：由 1m K 聚合。
- 聚合结果可写临时 Parquet/JSON cache，但不回写主库。

这样做的好处：

- 保留最小原始真值。
- 避免多周期数据冗余。
- 易于重算。
- 便于控制存储。

性能关键：

- 日线阶段不碰分钟库。
- 分钟阶段只读取 `watchlist × trade_date`。
- 禁止启动回测时加载多年全市场分钟线。

## 15. 常用 API

### 15.1 策略

```text
POST /v1/strategies/llm-draft
GET  /v1/strategies/dsl-schema
GET  /v1/strategies/templates
GET  /v1/strategies/templates/{template_id}
POST /v1/strategies
GET  /v1/strategies
GET  /v1/strategies/{id}
POST /v1/strategies/{id}/compile
GET  /v1/strategies/{id}/versions
POST /v1/strategies/{id}/versions
POST /v1/strategies/{id}/clone
POST /v1/strategies/{id}/activate
```

### 15.2 回测

```text
POST /v1/backtests
GET  /v1/backtests/{id}
POST /v1/backtests/{id}/cancel
GET  /v1/backtests/{id}/metrics
GET  /v1/backtests/{id}/equity
GET  /v1/backtests/{id}/trades
GET  /v1/backtests/{id}/positions
GET  /v1/backtests/{id}/signals
GET  /v1/backtests/{id}/orders
GET  /v1/backtests/{id}/watchlists
GET  /v1/backtests/{id}/minute-confirmations
GET  /v1/backtests/{id}/trade-snapshots
POST /v1/backtests/compare
```

### 15.3 进化

```text
POST /v1/evolution/experiments
GET  /v1/evolution/experiments/{id}
GET  /v1/evolution/experiments/{id}/candidates
POST /v1/evolution/candidates/{id}/accept
```

### 15.4 纸交易

```text
POST /v1/paper/accounts
POST /v1/paper/accounts/{id}/run-strategy
GET  /v1/paper/accounts/{id}/orders
GET  /v1/paper/accounts/{id}/positions
GET  /v1/paper/accounts/{id}/equity
```

## 16. 推荐实操示例

### 示例一：创建算力波段策略

1. 打开 `/strategies/create`。
2. 选择“算力业绩高增波段”模板。
3. 设置：
   - 选股数量：30
   - 最低评分：0.65
   - 最大持仓数：20
   - 单笔风险：0.01
4. 输入白话补充：

```text
只要算力、AI 服务器、数据中心方向，优先净利润增长强、资金流强的中盘股。
```

5. 点击“生成 DSL 草案”。
6. 检查待确认项。
7. 点击“保存并进入回测”。

### 示例二：运行日线 + 分钟确认回测

1. 打开 `/backtest`。
2. 选择策略。
3. 频率选择 `daily_minute`。
4. 开启分钟确认。
5. 设置区间：

```text
2024-01-01 ~ 2024-12-31
```

6. 点击运行回测。
7. 查看：
   - 资金曲线
   - 回撤曲线
   - 候选池明细
   - 分钟确认明细
   - 订单生命周期
   - Trade Snapshot

### 示例三：创建进化实验

1. 回到 `/strategies`。
2. 选择刚回测完成的策略。
3. 点击“发起进化”。
4. 查看候选策略。
5. 如果候选策略风险提示可接受，点击接受。
6. 系统生成新版本。
7. 对新版本再次回测。

### 示例四：进入纸交易

1. 在策略管理页选择策略。
2. 点击“运行纸交易”。
3. 查看模拟订单。
4. 查看模拟持仓。
5. 查看权益曲线。

## 17. 常见问题

### 17.1 为什么没有分钟确认？

可能原因：

- 回测频率选择了 `daily`。
- 策略 DSL 没有 `intraday_confirm`。
- 分钟数据缺失。
- 当前标的当天没有满足确认条件。

### 17.2 为什么显示 fallback？

可能原因：

- 当前环境没有安装 DuckDB。
- 当前环境没有安装 Polars。
- Parquet 数据不存在。
- 真实数据库查询失败，使用了 synthetic fallback。

### 17.3 为什么没有交易？

可能原因：

- `min_score` 过高。
- 股票池过滤过严。
- 分钟确认没有命中。
- 风控限制阻止下单。
- 现金或容量不足。

### 17.4 候选策略会不会覆盖原策略？

不会。

候选策略接受后应生成新版本或新策略副本，原策略保留。

### 17.5 纸交易是真实交易吗？

不是。

当前边界是信号和模拟订单，不直接连接真实券商交易。

## 18. 最佳实践

- 先用日线模式快速验证策略方向。
- 再用 `daily_minute` 模式验证入场质量。
- 不要一开始就用过多因子，避免过拟合。
- 使用 Walk-forward 验证参数稳定性。
- 进化候选必须重新回测，不能直接激活。
- 纸交易至少观察多个市场阶段。
- 每次策略改动都生成新版本，保留历史。
- 大回测优先使用 Parquet + DuckDB + Polars 真链路。

## 19. 完整状态验收标准

完整状态下，应满足：

- 策略可从模板、白话、手动 DSL 三种方式创建。
- LLM 输出必须满足 Pydantic JSON Schema。
- DSL 编译失败不能保存为可运行策略。
- 日线全市场扫描走 DuckDB/Polars。
- 分钟线只按 Watchlist 懒加载。
- A 股规则完整执行。
- 回测大结果写 artifact，不塞大 JSON。
- 所有明细接口支持分页、筛选和排序。
- Trade Snapshot 可用于策略进化。
- 进化候选必须用户确认后生成新版本。
- 纸交易可复用回测信号与订单。
- 前端能完成“创建 → 回测 → 进化 → 纸交易”的完整闭环。
