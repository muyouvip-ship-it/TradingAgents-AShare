# 策略管理与回测模块未完成项修复清单

更新时间：2026-04-21

## 1. 判断口径

- 参照文档：`策略管理与回测模块重构设计方案.md`
- 当前状态：主链路已完成，策略创建、模板、DSL、回测、分钟确认、进化、纸交易均已具备可用闭环。
- 本清单只列“距离完整生产级目标仍有差距”的事项。
- 排序规则：
  - **优先级**：P0 > P1 > P2 > P3
  - **工作量**：S < M < L < XL
  - **推荐执行顺序**：先做影响生产可靠性的 P0，再做策略能力深度。

## 2. 总体完成度判断

| 模块 | 当前完成度 | 状态 |
|---|---:|---|
| 策略管理 | 92% | 主链路完成，已支持模板/白话/DSL 三种创建方式，缺少更完整可视化编辑器 |
| 策略模板 | 92% | 已有模板接口、模板参数、模板来源持久化，作用已在创建页明确化 |
| LLM 策略创建 | 55% | 有结构化 DSL 契约，但草案生成仍偏规则/模板化 |
| DSL 与编译器 | 75% | 有 Pydantic 强约束和执行 IR，但 Polars Expr 泛化不足 |
| 日线回测引擎 | 85% | 真引擎可用，仍需异步任务和更强一致性测试 |
| 分钟线懒加载 | 80% | 已按 Watchlist 懒加载，需补更多真实分钟数据边界测试 |
| A 股撮合规则 | 85% | T+1、整手、涨跌停、停牌、费用、容量已具备，行业暴露待补 |
| 仓位与风控 | 80% | 多仓位模型已具备，行业仓位/分批建仓细节待增强 |
| 进化实验 | 65% | 有 Trade Snapshot 归因候选，缺少完整搜索器和过拟合标注 |
| 纸交易 | 75% | 账户、订单、持仓、权益闭环可用，缺少调度与策略持续运行 |
| 数据库迁移 | 40% | 当前能初始化表，缺少 Alembic 迁移体系 |
| 前端工作台 | 84% | 主 UI 可用，创建页已支持多入口创建，策略编辑器和进化工作台可继续增强 |

## 3. P0：生产可靠性必修

### 1. 接入真实 LLM 策略生成

- **优先级**：P0
- **工作量**：L
- **原因**：当前 `/v1/strategies/llm-draft` 已返回合法 DSL 和 Schema，但主要是规则化/模板化草案，不是真正的自然语言策略生成。
- **目标**
  - 接入真实 LLM Provider，并复用设置页已有模型配置。
  - 策略管理、策略回测、进化解释相关 LLM 不单独维护模型配置。
  - 使用当前用户的 `llm_provider`、`quick_think_llm`、`deep_think_llm`、`backend_url`、`api_key`。
  - 请求时传入 `StrategyDslSchema.model_json_schema()`。
  - 强制 Structured Outputs。
  - 支持待确认项、未知字段、未知因子、未知操作符回传。
- **验收标准**
  - 白话输入不同策略条件时，DSL 能随语义变化。
  - 登录用户修改设置页模型后，策略 LLM 草案接口立即使用同一配置。
  - 响应中只返回脱敏模型信息，不返回明文 API Key。
  - 非法/模糊需求不静默保存，必须返回待确认项。
  - 单测覆盖：选股策略、交易策略、风控策略、组合策略。

### 2. 回测任务改为异步任务队列

- **优先级**：P0
- **工作量**：L
- **原因**：当前 `POST /v1/backtests` 同步执行，`progress` 基本固定为 `1.0`，长回测会阻塞请求。
- **目标**
  - 创建任务后立即返回 `pending/running`。
  - 后台执行回测。
  - 支持状态轮询、取消、失败重试、错误日志。
  - 前端展示真实进度。
- **验收标准**
  - 大回测不会阻塞 HTTP 请求。
  - `/v1/backtests/{id}` 可看到阶段进度。
  - 取消任务能停止后续阶段。

### 3. 回测结果彻底 Artifact-only

- **优先级**：P0
- **工作量**：M
- **原因**：方案要求大结果不要写入 `backtest_jobs.result`，当前仍写入部分 `details` 子集。
- **目标**
  - PostgreSQL 只保存任务状态、摘要指标、artifact 路径。
  - 明细全部从 `data/artifacts/backtests/{run_id}/` 分页读取。
  - 前端所有明细接口走分页 API。
- **验收标准**
  - `backtest_jobs.result` 不再包含大数组。
  - 交易、资金、持仓、订单、候选池、分钟确认均可从 artifact 分页读取。

### 4. 引入 Alembic 迁移体系

- **优先级**：P0
- **工作量**：M
- **原因**：设计方案明确要求 Alembic；当前主要依赖 `create_all/init_db`，生产升级风险高。
- **目标**
  - 增加 Alembic 配置。
  - 为策略表、版本、回测、进化、纸交易、因子注册表生成初始迁移。
  - 后续字段变更通过迁移管理。
- **验收标准**
  - 新环境可通过迁移初始化。
  - 旧库可平滑升级。
  - CI 增加 migration smoke test。

## 4. P1：策略能力深水区

### 5. 完整 DSL → Polars Expr 编译器

- **优先级**：P1
- **工作量**：XL
- **原因**：当前 DSL 编译器已输出执行 IR 和表达式预览，但核心计算仍部分硬编码在 `strategy_compute_backend`。
- **目标**
  - 因子注册表里的 `polars_expr` 变成真实执行表达式。
  - 支持滚动窗口、横截面排名、行业内排名、相关系数、分位数。
  - 自定义因子走注册表扩展，而不是改计算后端。
- **验收标准**
  - 新增因子只需注册元数据和表达式。
  - DSL 编译产物可直接驱动 Polars 计算。
  - 小样本与 pandas 参考结果一致。

### 6. 行业仓位上限与行业暴露诊断

- **优先级**：P1
- **工作量**：M
- **原因**：DSL 中已有 `max_industry_position_pct`，但执行器尚未完整拦截行业暴露。
- **目标**
  - 下单前计算行业/概念持仓暴露。
  - 超行业上限时拒单或降仓。
  - 回测诊断输出行业暴露曲线。
- **验收标准**
  - 单测覆盖行业超限拒单。
  - 前端展示行业暴露摘要。

### 7. 分批建仓模型细化

- **优先级**：P1
- **工作量**：M
- **原因**：金字塔加仓已完成，但“初始仓位、确认仓位、突破加仓”的分批建仓模型仍可细化。
- **目标**
  - 支持 `initial/add/confirm/breakout` 多阶段仓位。
  - 每阶段可配置触发条件和比例。
  - 订单记录体现建仓阶段。
- **验收标准**
  - 回测订单可区分初始建仓、确认加仓、突破加仓。
  - DSL 可配置每阶段比例。

### 8. 分钟线退出确认与更细撮合

- **优先级**：P1
- **工作量**：L
- **原因**：当前分钟线重点用于入场确认，出场和更细成交仍偏日线。
- **目标**
  - 支持分钟线止损、止盈、跌破 VWAP、尾盘退出。
  - 支持日内成交价选择：open/close/vwap/next_bar_open。
  - 支持分钟级滑点和容量。
- **验收标准**
  - `daily_minute` 模式下，分钟出场能改变交易结果。
  - 不允许全市场分钟线预加载。

### 9. 进化模块升级为“搜索 + 归因 + 样本外验证”

- **优先级**：P1
- **工作量**：XL
- **原因**：当前已有 Trade Snapshot 归因候选，但还不是完整进化实验引擎。
- **目标**
  - 支持参数搜索、因子权重优化、阈值优化。
  - 每个候选必须回测。
  - 支持样本内/样本外/Walk-forward 对比。
  - 标记过拟合风险。
- **验收标准**
  - 候选列表包含 base vs candidate 指标对比。
  - 样本外退化候选不得推荐激活。
  - 接受候选只生成新版本，不覆盖原策略。

## 5. P2：前端产品体验增强

### 10. 策略可视化 Builder

- **优先级**：P2
- **工作量**：L
- **原因**：当前创建页已有模板、参数和 DSL 预览，但不是完整可视化编辑器。
- **目标**
  - 可视化编辑股票池、因子、入场、出场、仓位、风控。
  - 修改 Builder 自动同步 DSL。
  - DSL 校验错误定位到具体表单项。
- **验收标准**
  - 用户无需手写 JSON 即可创建复杂策略。
  - Builder 与 DSL 预览双向一致。

### 11. 进化工作台完整 UI

- **优先级**：P2
- **工作量**：M
- **原因**：策略管理页已有进化入口和候选展示，但独立进化工作台还可增强。
- **目标**
  - 展示目标函数、搜索空间、候选对比、过拟合风险。
  - 支持接受/拒绝候选。
  - 支持进入回测或纸交易。
- **验收标准**
  - 从策略详情可完整完成“创建实验 → 看候选 → 接受 → 新版本”流程。

### 12. 回测工作台补月度收益与分阶段视图

- **优先级**：P2
- **工作量**：M
- **原因**：当前已有资金曲线、回撤、收益分布、明细表，但方案中的月度收益和 IS/OOS 阶段视图还可补齐。
- **目标**
  - 月度收益热力图。
  - 样本内/样本外分段指标。
  - Walk-forward 每窗口结果展示。
- **验收标准**
  - Walk-forward 回测能展示每个窗口的训练/测试表现。

### 13. 纸交易策略持续运行

- **优先级**：P2
- **工作量**：L
- **原因**：当前纸交易可根据最近回测/信号生成订单，但不是持续运行策略调度。
- **目标**
  - 纸交易账户绑定策略版本。
  - 支持每日/每分钟定时运行。
  - 保存纸交易信号、订单、成交、持仓、权益曲线。
- **验收标准**
  - 策略可在纸账户持续运行。
  - 可暂停/恢复纸交易策略。

## 6. P3：工程质量和性能增强

### 14. 端到端真实数据集成测试

- **优先级**：P3
- **工作量**：M
- **目标**
  - 使用真实 PostgreSQL + Parquet + DuckDB + Polars 跑完整回测。
  - 覆盖日线、分钟线、artifact、分页接口。

### 15. 性能基准体系完善

- **优先级**：P3
- **工作量**：M
- **目标**
  - 全市场日线 20 年 benchmark。
  - 200 只股票 1 年分钟线 benchmark。
  - 进化批量候选 benchmark。
  - 输出耗时、内存、缓存命中率。

### 16. 观测与诊断增强

- **优先级**：P3
- **工作量**：S
- **目标**
  - 记录每次回测的阶段耗时。
  - 记录 DuckDB 扫描行数、Polars 计算行数、分钟加载次数。
  - 前端诊断页展示性能瓶颈。

## 7. 推荐执行路线

### 第一轮：生产可靠性

1. 回测任务异步化
2. Artifact-only 瘦身
3. Alembic 迁移
4. 真实 LLM 接入

### 第二轮：策略能力补深

1. DSL → Polars Expr 泛化
2. 行业仓位上限
3. 分批建仓模型
4. 分钟线退出确认

### 第三轮：进化与产品体验

1. 完整进化搜索器
2. 进化工作台
3. 策略可视化 Builder
4. Walk-forward 分阶段前端视图

### 第四轮：性能与验收

1. 真实数据端到端测试
2. 全市场性能基准
3. 观测诊断
4. 文档与用户手册收口
