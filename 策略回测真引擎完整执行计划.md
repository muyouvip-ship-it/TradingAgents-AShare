# 策略回测真引擎完整执行计划

## Summary
- 目标文档路径固定为 `/Users/wolf/Documents/DaiMa/TradingAgents-AShare-main/策略回测真引擎完整执行计划.md`，内容以本计划为准。
- 总体执行顺序固定为：`文档固化 → 编译器 IR → 日线真引擎 → 1m 通用聚合/懒加载 → 两阶段回测 → 前端回显 → 进化闭环 → 性能压测与收口`。
- 主数据策略长期保留：`stock_daily_kline + stock_minute_kline`。
- 周期规划定稿：
  - `日 K` 作为全市场向量化扫描主表
  - `1m K` 作为分钟层唯一原始真值
  - `5m/15m/30m/周线` 均通过 `1m` 或 `日 K` 运行时合成，不进入 PostgreSQL 主库长期存储
- 性能设计原则固定：
  - 日线阶段只扫描 `stock_daily_kline`
  - 分钟阶段只对 `watchlist × trade_date` 懒加载 `1m`
  - 30m 等周期通过 `1m` 聚合，并写运行时 artifact / cache，不回写 PostgreSQL 主表

## Implementation Changes

### 1. 文档与基线固化
- 将现有设计方案与“真引擎优先”计划统一为一份执行文档。
- 文档明确：
  - 主数据策略：`stock_daily_kline + stock_minute_kline`
  - 其他周期来源：全部由 `1m` 或 `日 K` 重采样合成
  - 不新增长期存储的 `5m/15m/30m` 表
  - fallback 与真引擎的切换条件
- 同步记录当前环境事实：
  - `duckdb/polars/pyarrow/sklearn` 当前未安装
  - 当前系统仍需保留 `pandas/sqlalchemy fallback`

### 2. DSL 编译器升级为执行 IR
- 将 `compile_strategy_dsl()` 升级为“执行性 IR”。
- IR 输出固定包含：
  - `factor_definitions`
  - `selection_plan`
  - `entry_rule_plan`
  - `exit_rule_plan`
  - `timeframes_required`
  - `minute_requirements`
  - `backend_resolution`
- 因子定义固定字段：
  - `name/source_column/weight/direction/transform/window/rank_scope/backend`
- 条件规则固定字段：
  - `type/timeframe/rule_key/params/data_requirements`
- 编译器固定约束：
  - `factor_model.select.min_score ∈ [0,1]`
  - `lot_size=100` 默认 A 股强约束
  - `fill_timing=next_open`
  - `intraday_confirm` 只能引用分钟字段
- `/v1/strategies/{id}/compile` 返回值扩展但不改路径。

### 3. 日线真引擎落地
- 抽出日线执行层，形成统一入口：
  - 输入：策略 DSL IR、symbols、start/end、capital
  - 输出：daily feature matrix、watchlist、signals、diagnostics
- 计算后端固定分层：
  - DuckDB：日期过滤、symbol 过滤、Join、裁剪
  - Polars：rolling、pct_change、横截面 rank、weighted score
  - pandas：仅 fallback
- 保留 `stock_daily_kline` 为全市场主扫描表，不从 `1m` 回推日 K。
- 周线趋势规则第一版固定为：
  - 由日 K 重采样生成周线序列
  - 不从 `1m` 直接生成周线
- 回测摘要新增：
  - `engine_mode`
  - `compiled_factor_count`
  - `watchlist_days`
  - `backend_resolution`

### 4. 1m 通用聚合与懒加载
- 新增分钟数据访问层，统一负责从 `stock_minute_kline` 读取 `1m` 原始数据。
- 聚合策略固定：
  - 只基于 `1m` 生成 `5m/15m/30m`
  - 输出字段固定：`open/high/low/close/volume/amount/vwap/bar_start/bar_end`
- 聚合范围固定：
  - 只针对 `trade_date + watchlist symbols`
  - 不允许回测启动时全量扫多年分钟线
- 缓存策略固定：
  - 不新增长期 PostgreSQL 聚合表
  - 允许写 `data/artifacts/minute_cache/` 或 backtest run artifact
  - 缓存粒度固定为 `symbol + trade_date + timeframe`
- 当前 DSL 中 `30m VWAP 确认` 的执行定义固定为：
  - `1m -> 30m` 聚合
  - 计算 30m VWAP
  - 判断 `close cross above vwap`
- 若分钟数据缺失：
  - `daily` 模式直接跳过分钟层
  - `daily_minute` 模式记录 `minute_data_missing`
  - 默认该标的当日不触发分钟确认买点

### 5. 两阶段回测引擎
- 回测引擎固定拆成两个阶段：
  - 阶段 A：日线全市场/候选池向量化
  - 阶段 B：分钟层事件驱动确认
- 日线阶段职责：
  - universe/filter
  - 多因子评分
  - weekly trend / daily setup
  - 生成 watchlist
- 分钟阶段职责：
  - 逐日读取 watchlist 对应 `1m`
  - 聚合出 `30m`
  - 做 intraday confirm
  - 输出最终 buy/sell signal
- A 股执行规则统一收敛到一个执行器：
  - T+1
  - 100 股整数手
  - 涨跌停不可成交
  - 停牌跳过
  - 手续费/印花税/滑点
  - `next_open` 成交
- Artifact 固定输出：
  - `compiled_strategy.json`
  - `metrics.json`
  - `equity.parquet|json`
  - `trades.parquet|json`
  - `trade_snapshots.parquet|json`
  - `watchlists.parquet|json`
  - `minute_confirmations.parquet|json`
  - `signals.parquet|json`
  - `positions.parquet|json`
  - `engine_diagnostics.json`

### 6. 前端回显与调试信息
- 保持页面路由不变：`/strategies`、`/strategies/create`、`/backtest`
- 创建页新增回显：
  - `timeframes_required`
  - `backend_resolution`
  - `minute_requirements`
- 回测页新增展示：
  - 真引擎 / fallback 状态
  - 分钟聚合周期
  - watchlist 数量
  - 分钟确认命中率
  - 分钟数据缺失数
  - fallback warning
- 如果当前运行走 fallback，前端固定提示：
  - “当前结果用于流程验证；性能与精度以真引擎环境为准”

### 7. 自进化闭环承接
- 不在本阶段优先做复杂模型搜索，但要保证真引擎输出能喂给进化模块。
- 每笔 entry/sell 继续写 `Trade Snapshot`。
- Snapshot 中固定保留：
  - 因子向量
  - 横截面 rank
  - market state
  - minute confirm result
  - future_return_labels
- 进化模块继续读取最近一次完成回测的 snapshot，生成候选策略。
- 候选接受后必须：
  - 复制 base strategy
  - 应用 `dsl_patch`
  - 形成新版本
  - 不覆盖原策略

### 8. 性能与数据策略定稿
- 数据保留策略最终定稿：
  - 长期保留：`日 K + 1m K`
  - 不长期保留：`5m/15m/30m/周线`
- 性能原则固定：
  - 日线扫描看 `stock_daily_kline`
  - 分钟确认只读 `stock_minute_kline`
  - 聚合结果只做临时缓存，不回主库
- 若未来分钟确认使用频率极高，再追加可选优化：
  - 离线预计算最近 N 天热点股票 `30m parquet cache`
  - 但仍不改主数据策略

## Public Interfaces
- 保持现有接口路径不变：
  - `POST /v1/strategies/{id}/compile`
  - `POST /v1/backtests`
  - `GET /v1/backtests/{id}`
  - `GET /v1/backtests/{id}/equity`
  - `GET /v1/backtests/{id}/trades`
  - `GET /v1/backtests/{id}/trade-snapshots`
  - `GET /v1/backtests/{id}/signals`
  - `GET /v1/backtests/{id}/positions`
- 扩展返回字段：
  - `compile`：`execution_plan/timeframes_required/backend_resolution/minute_requirements`
  - `backtest.result.summary`：`engine_mode/minute_aggregation/watchlist_days`
  - `backtest.result.diagnostics`：`fallback_mode/minute_load_count/minute_symbol_days/confirm_hit_rate/minute_data_missing`

## Test Plan
- 编译器
  - 空因子、非法阈值、分钟规则缺字段时报错
  - `30m` 条件能正确产出 timeframe 需求
- 日线真引擎
  - Polars 与 pandas fallback 在小样本上一致
  - 横截面 rank、rolling、weighted sum 一致
- 分钟聚合
  - `1m -> 5m/15m/30m` OHLCV/VWAP 正确
  - 只读取 watchlist 对应 symbol/day
  - 禁止一次性加载全市场多年分钟线
- 两阶段回测
  - `daily` 模式不触发分钟读取
  - `daily_minute` 模式触发分钟确认并改变买点
  - A 股规则：T+1、整手、涨跌停、停牌、费用 全覆盖
- 前端
  - `npm run build` 通过
  - 编译页能展示后端与 timeframe 需求
  - 回测页能展示真引擎/ fallback、分钟统计与诊断

## Assumptions
- 当前库中真实存在 `stock_daily_kline` 和 `stock_minute_kline`；分钟表以 `1m` 为唯一原始真值。
- 当前未发现现成 `stock_minute_kline` ORM 模型；实现时允许新增独立 service 或 SQLAlchemy Core 访问层。
- 当前环境缺少 `duckdb/polars/pyarrow/sklearn`，因此第一轮实现必须支持自动 fallback。
- 关于数据规划结论：`日 K + 1m K` 是对的，既保真也便于控制存储；性能能做上来，但关键不在“少存周期表”，而在“日线与分钟层严格分治 + 1m 懒加载 + 聚合结果不回主库”。
