# 回测数据订阅与增量更新流程说明

## 1. 目标

回测数据配置不再只是“手动下载一次”，而是升级为**数据订阅**：

- 每天定点执行；
- 只拉取最新增量；
- 自动生成下载任务；
- 下载完成后更新水位；
- 下次继续从上次成功位置往后追；
- 支持交易日限制；
- 支持不同数据源（如 `quantclass` / `akshare` / `qmt`）。

---

## 2. 现在的完整链路

### 2.1 设置页配置订阅

前端设置页已支持以下订阅项：

- `启用自动更新`
- `更新频率`：`daily / weekly / monthly`
- `执行时间`：如 `18:30`
- `时区`：默认 `Asia/Shanghai`
- `仅交易日执行`
- `数据类型`
- `数据源`
- `默认回溯天数`

保存后写入：

- `backtest_data_configs`

核心字段：

- `auto_download`
- `update_frequency`
- `schedule_time`
- `timezone`
- `only_trading_day`
- `enabled_data_types`
- `data_source_preference`
- `default_date_range_days`

---

### 2.2 应用启动后自动拉起后台订阅 worker

应用启动时：

- `api/lifespan.py`

会启动：

- `backtest_data_auto_update_service.start_background_worker()`

该 worker 每 60 秒轮询一次订阅配置。

---

### 2.3 到点判断

后台 worker 会检查：

- 当前是否到了 `schedule_time`
- 是否满足 `only_trading_day`
- 今天是否已经跑过
- `daily/weekly/monthly` 是否满足执行周期

判断逻辑位置：

- `api/services/backtest_data_auto_update_service.py`

核心函数：

- `_should_run()`

---

### 2.4 增量范围计算

系统不是每次全量重拉，而是先查水位表：

- `backtest_data_watermarks`

按以下维度定位一条订阅增量游标：

- `user_id`
- `config_id`
- `data_type`
- `data_source`
- `scope_key`

其中：

- 如果是全市场，`scope_key=all`
- 如果是指定股票池，`scope_key=symbols:...`

增量计算逻辑：

- 若存在 `last_data_date`，则下次从 `last_data_date + 1` 开始；
- 若不存在水位，则首次按 `default_date_range_days` 回补；
- 结束日期默认取当前交易日/当前日期。

---

### 2.5 自动创建下载任务

满足调度条件后，系统会自动向：

- `backtest_data_tasks`

插入新任务，并打上：

- `subscription_config_id`
- `trigger_mode=scheduled`

然后后台线程直接调用：

- `_process_batch_download()`

去执行真实下载。

---

### 2.6 下载完成后更新统计与水位

任务执行完成后，会同步更新：

- `backtest_data_stats`
- `backtest_data_watermarks`
- `backtest_data_configs.last_success_at`

这样下一次订阅执行时，就会自动从新的成功水位继续增量更新。

---

## 3. 关键数据表职责

### 3.1 `backtest_data_configs`

订阅主配置表，存“怎么订”。

### 3.2 `backtest_data_tasks`

任务执行表，存“每次跑了什么任务”。

### 3.3 `backtest_data_watermarks`

增量游标表，存“已经拉到哪里了”。

### 3.4 `backtest_data_stats`

统计表，存“目前库里有什么数据、最新到哪天”。

---

## 4. 你要的目标流程

你想要的流程，建议固定为下面这套：

### 4.1 日 K 订阅

- 每天 `18:30` 执行；
- 仅交易日执行；
- 增量拉取最近未落库的日 K；
- 更新 `stock_daily_kline`；
- 写入最新水位。

### 4.2 1 分钟 K 订阅

分两段：

- **盘后补全**：每天收盘后从数据源增量拉取当日 1 分钟 K；
- **盘中实时落库**：若开启实时监控，则通过 QMT 行情实时抓取当天 1 分钟 K 并写入 `stock_minute_kline`。

也就是说：

- **盘后订阅**负责稳定补数；
- **实时监控链路**负责盘中持续写入。

---

## 5. 当前已落地的部分

- 设置页已增加订阅配置项；
- 后端配置接口已支持订阅字段；
- 后台自动调度 worker 已接入；
- 已支持按固定时间执行；
- 已支持交易日过滤；
- 已支持基于 watermark 的增量计算；
- 已支持下载完成后更新水位；
- 实时监控已接入 QMT 分钟线抓取并写库。

---

## 6. 当前还需要注意的点

### 6.1 后端进程必须常驻

当前订阅机制依赖应用内后台 worker，因此：

- 后端服务必须持续运行；
- 如果服务停了，到点不会自动触发；
- 服务恢复后会在下一轮扫描时补跑当天未执行的订阅。

### 6.2 QMT 分钟线是否有数据取决于桥接端真实返回

目前代码链路已经打通，但如果 QMT 端对某些日期/标的不返回分钟线，任务仍可能是 0 行。

这不是调度层问题，而是数据源返回结果问题。

### 6.3 当前日内 1m 自动入库依赖实时监控链路

如果你希望**不开实时监控也能盘中持续写 1m**，后面可以再补一个独立的：

- `QMT 行情订阅采集服务`

让它脱离策略监控独立运行。

---

## 7. 推荐默认配置

### 7.1 日 K

- 数据类型：`daily_kline`
- 数据源：`quantclass` 或稳定日线源
- 自动更新：开启
- 频率：`daily`
- 时间：`18:30`
- 仅交易日：开启

### 7.2 1 分钟 K

- 数据类型：`minute_kline`
- 数据源：`qmt`
- 自动更新：开启
- 频率：`daily`
- 时间：`15:10` 或 `18:00`
- 仅交易日：开启

---

## 8. 建议的下一步

下一步建议继续补下面 3 项：

1. 给设置页增加“下次执行时间 / 上次成功时间 / 当前水位”展示；
2. 给下载任务列表增加“手动立即执行一次订阅”；
3. 将盘中 QMT 1m 实时采集独立成常驻采集服务，而不是只挂在实时监控里。

