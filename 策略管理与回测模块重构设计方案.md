# 策略管理与回测模块重构设计方案

## 1. 设计目标

本方案用于重新实现项目中的策略管理与策略回测模块，目标是形成一个面向 A 股市场的高性能、可解释、可进化的策略平台。

第一版定位为“生产级核心”，优先重建以下能力：

- 灵活策略创建：支持选股策略、交易策略、风控策略、组合策略。
- LLM 策略创建：把自然语言需求转成可解释、可审计、可验证的策略 DSL。
- A 股规则适配：覆盖 T+1、100 股整数手、涨跌停、停牌、费用、滑点等交易约束。
- 高性能回测：支持几千只股票、多年日线数据，以及候选股票池的分钟线增强回测。
- 仓位管理：同一套仓位/风控规则可用于回测和纸交易。
- 策略进化：自动生成候选优化方案，但必须用户确认后才激活。
- 核心重点：选股 + 多周期波段交易。

## 2. 总体架构

### 2.1 模块拆分

建议将策略与回测重构为以下核心模块：

1. **策略管理模块**
   - 策略定义
   - 策略版本
   - 策略 DSL
   - 策略编译与校验
   - 策略模板与克隆

2. **LLM 策略创建模块**
   - 自然语言解析
   - 意图结构化
   - DSL 草案生成
   - 数据依赖分析
   - 风险提示
   - 用户确认保存

3. **数据与因子模块**
   - PostgreSQL 元数据
   - Parquet 行情/因子缓存
   - DuckDB 高速扫描
   - 日线、周线、分钟线多周期数据
   - 因子注册、计算、缓存

4. **回测引擎模块**
   - 数据加载器
   - 策略 DSL 编译器
   - 信号生成器
   - 事件驱动撮合器
   - 组合估值器
   - 绩效分析器

5. **仓位与风控模块**
   - 单票仓位
   - 组合仓位
   - 行业暴露
   - 止损止盈
   - 熔断降仓
   - 风控拦截

6. **进化实验模块**
   - 参数优化
   - 因子权重优化
   - 规则阈值优化
   - 风控叠加
   - 样本内/样本外验证
   - 候选策略确认

7. **纸交易模块**
   - 模拟账户
   - 模拟订单
   - 模拟成交
   - 模拟持仓
   - 策略信号落地

### 2.2 数据计算层选型

采用 `PostgreSQL + Parquet + Polars + DuckDB`：

- **PostgreSQL**
  - 存储用户、策略、版本、任务、指标摘要、权限、纸交易账户等元数据。
  - 适合事务、查询、任务状态管理。

- **Parquet**
  - 存储大规模行情、因子矩阵、资金曲线、交易明细、持仓快照等大结果。
  - 避免把大量数组写入 PostgreSQL JSON 字段。

- **Polars**
  - 作为策略 DSL 的核心计算后端。
  - DSL 编译器优先把因子、条件、横截面排名、多周期滚动窗口编译为 Polars Expr。
  - 适合处理滚动均线、相关系数、行业内分位数、多因子打分、全市场信号矩阵。
  - 对时序 + 截面混合计算比纯 SQL 更自然，也更容易维护。

- **DuckDB**
  - 对 Parquet 做高速过滤、裁剪、Join、聚合和数据搬运。
  - 适合作为数据查询层，把回测所需切片高效交给 Polars。
  - 不承担复杂策略逻辑的唯一计算职责，避免把复杂因子写成难维护 SQL。

## 3. 策略 DSL 设计

### 3.1 为什么不用字符串条件

当前项目里已有简单条件表达式，例如：

```text
cross_above(ma5, ma20)
macd > signal AND volume > volume_ma * 1.5
```

这种方式的问题：

- 很难扩展多因子、多周期、行业/概念、资金流、财务字段。
- 字符串替换容易误替换变量。
- 很难做未来函数检测。
- 很难让 LLM 稳定生成。
- 很难做策略版本 diff 和可解释展示。

建议改为 JSON AST DSL。

### 3.2 DSL 顶层结构

```json
{
  "schema_version": "1.0",
  "strategy_type": "portfolio",
  "universe": {},
  "factor_model": {},
  "entry": {},
  "exit": {},
  "position": {},
  "risk": {},
  "execution": {},
  "evolution": {}
}
```

### 3.3 核心块说明

#### universe：股票池

用于定义选股范围。

支持：

- 全 A
- 指数成分股
- 自选股
- 行业
- 概念
- 市值范围
- ST 排除
- 停牌排除
- 上市天数过滤
- 财务质量过滤

示例：

```json
{
  "universe": {
    "market": "A_SHARE",
    "include_concepts": ["算力", "AI算力", "数据中心"],
    "exclude_st": true,
    "exclude_suspended": true,
    "min_listing_days": 250,
    "filters": [
      {
        "field": "float_market_cap",
        "op": "between",
        "value": [10000000000, 20000000000],
        "unit": "CNY"
      }
    ]
  }
}
```

#### factor_model：多因子模型

用于选股排序和评分。

支持：

- 估值因子
- 成长因子
- 质量因子
- 动量因子
- 波动率因子
- 资金流因子
- 量价因子
- 行业/概念强度因子

示例：

```json
{
  "factor_model": {
    "score_method": "weighted_sum",
    "rebalance_frequency": "weekly",
    "factors": [
      {
        "name": "net_profit_growth_yoy",
        "weight": 0.35,
        "direction": "higher_better",
        "transform": "rank_pct"
      },
      {
        "name": "money_flow_strength_20d",
        "weight": 0.25,
        "direction": "higher_better",
        "transform": "rank_pct"
      },
      {
        "name": "momentum_60d",
        "weight": 0.25,
        "direction": "higher_better",
        "transform": "rank_pct"
      },
      {
        "name": "volatility_20d",
        "weight": 0.15,
        "direction": "lower_better",
        "transform": "rank_pct"
      }
    ],
    "select": {
      "top_n": 30,
      "min_score": 0.65
    }
  }
}
```

#### entry：入场条件

用于定义买入或建仓条件。

多周期波段交易建议默认：

- 周线判断中期趋势。
- 日线判断波段信号。
- 分钟线确认入场点。

示例：

```json
{
  "entry": {
    "logic": "all",
    "conditions": [
      {
        "type": "trend",
        "timeframe": "1w",
        "field": "close",
        "op": "above",
        "indicator": "ma20"
      },
      {
        "type": "alligator_opening",
        "timeframe": "1d",
        "params": {
          "jaw": 13,
          "teeth": 8,
          "lips": 5
        },
        "direction": "bullish"
      },
      {
        "type": "breakout",
        "timeframe": "1d",
        "field": "close",
        "lookback": 20,
        "op": "new_high"
      },
      {
        "type": "intraday_confirm",
        "timeframe": "30m",
        "conditions": [
          {
            "type": "cross_above",
            "left": "close",
            "right": "vwap"
          }
        ]
      }
    ]
  }
}
```

#### exit：出场条件

支持：

- 趋势破位
- 均线跌破
- ATR 止损
- 移动止损
- 达到目标收益
- 时间止损
- 因子排名跌出
- 风险熔断

示例：

```json
{
  "exit": {
    "logic": "any",
    "conditions": [
      {
        "type": "cross_below",
        "timeframe": "1d",
        "left": "close",
        "right": "ma20"
      },
      {
        "type": "atr_trailing_stop",
        "timeframe": "1d",
        "atr_period": 14,
        "atr_multiple": 2.5
      },
      {
        "type": "factor_rank_drop",
        "rank_below": 0.5
      }
    ]
  }
}
```

#### position：仓位管理

支持：

- 等权
- 波动率目标
- ATR 风险预算
- 分批建仓
- 金字塔加仓
- 最大单票仓位
- 最大组合仓位
- 行业上限

示例：

```json
{
  "position": {
    "method": "risk_budget",
    "initial_position_pct": 0.2,
    "max_position_pct": 0.8,
    "max_single_position_pct": 0.12,
    "max_industry_position_pct": 0.35,
    "cash_reserve_pct": 0.05,
    "risk_per_trade_pct": 0.01,
    "sizing_basis": "atr"
  }
}
```

#### risk：风控规则

示例：

```json
{
  "risk": {
    "stop_loss_pct": 0.08,
    "take_profit_pct": 0.25,
    "trailing_stop_pct": 0.1,
    "max_drawdown_pct": 0.15,
    "max_daily_loss_pct": 0.03,
    "max_positions": 20,
    "cooldown_days_after_stop": 3
  }
}
```

#### execution：执行与撮合

示例：

```json
{
  "execution": {
    "market": "A_SHARE",
    "signal_timing": "close",
    "fill_timing": "next_open",
    "price_mode": "open",
    "lot_size": 100,
    "tick_size": 0.01,
    "commission_rate": 0.0003,
    "stamp_duty_rate": 0.001,
    "slippage_model": {
      "type": "bps",
      "value": 10
    },
    "volume_limit_pct": 0.1
  }
}
```

#### evolution：进化约束

示例：

```json
{
  "evolution": {
    "enabled": true,
    "allowed_mutations": [
      "factor_weight",
      "threshold",
      "risk_overlay",
      "exit_rule"
    ],
    "objective": "calmar_then_win_rate",
    "max_complexity_score": 20,
    "require_user_confirmation": true
  }
}
```

## 4. LLM 策略创建设计

### 4.1 创建流程

用户输入：

```text
创建一个选股策略：算力板块、资金在 100 亿到 200 亿之间、业绩暴增的股票。
```

系统流程：

1. 提取意图。
2. 识别策略类型。
3. 识别股票池。
4. 识别字段和单位。
5. 映射到可用数据字段。
6. 生成 DSL 草案。
7. 编译校验。
8. 生成解释报告。
9. 用户确认后保存。

### 4.2 默认语义约定

- “资金 100 亿到 200 亿”默认解释为人民币流通市值 `float_market_cap`。
- 如果用户明确说“成交额”，则映射到 `amount`。
- 如果用户明确说“主力资金”，则映射到资金流字段。
- “业绩暴增”默认解释为净利润同比高增长。
- 如果缺少同比字段，则使用最近一期净利润增长衍生因子。

### 4.3 LLM 输出要求

LLM 不能直接生成可执行 Python。

LLM 必须使用 Pydantic JSON Schema / Structured Outputs 强约束输出。系统在调用 LLM 时需要把策略 DSL 的 Pydantic 模型定义传入模型，确保输出满足以下要求：

- JSON 语法合法。
- 必填字段不缺失。
- `op`、`timeframe`、`condition_type`、`factor_name` 等字段只能取枚举值。
- 不允许捏造系统不支持的指标、字段或操作符。
- LLM 输出先过 schema 校验，再过业务编译校验，最后才允许保存草稿。
- 如果业务校验失败，应返回“待确认项”或“不支持算子”，不得静默保存策略。

LLM 必须输出：

- 策略名称
- 策略类型
- 股票池条件
- 因子条件
- 入场条件
- 出场条件
- 仓位规则
- 风控规则
- 数据依赖
- 待确认项
- 风险提示
- DSL 草案

### 4.4 示例输出摘要

```json
{
  "name": "算力业绩高增中盘选股策略",
  "strategy_type": "selection",
  "intent_summary": "筛选算力相关板块中，流通市值 100-200 亿且业绩高速增长的股票。",
  "pending_confirmations": [
    {
      "field": "资金",
      "assumed_as": "float_market_cap",
      "reason": "用户描述为 100 亿到 200 亿，更接近市值区间。"
    }
  ]
}
```

## 5. A 股规则引擎

### 5.1 必须支持的规则

- T+1：当天买入不能当天卖出。
- 100 股整数手买入。
- 卖出允许处理剩余零股。
- 最小价格变动单位 0.01 元。
- 涨停不可买入。
- 跌停不可卖出。
- 停牌不可成交。
- 佣金。
- 卖出印花税。
- 滑点。
- 成交量容量限制。
- 不同市场板块涨跌幅差异。

### 5.2 规则配置化

不要把 A 股规则硬编码在引擎里，应建立规则配置。

示例：

```json
{
  "market": "A_SHARE",
  "board": "main",
  "lot_size": 100,
  "tick_size": 0.01,
  "t_plus": 1,
  "daily_limit_pct": 0.1,
  "st_daily_limit_pct": 0.05,
  "stamp_duty_side": "sell"
}
```

### 5.3 官方规则来源

规则实现时应以交易所公开规则为准，并保留配置更新能力：

- [上海证券交易所交易规则相关公开材料](https://www.sse.com.cn/aboutus/mediacenter/hotandd/c/c_20260410_10814807.shtml)
- [深圳证券交易所交易规则公开材料](https://docs.static.szse.cn/www/lawrules/rule/stock/trade/W020230217564423808793.pdf)
- [北京证券交易所业务规则](https://www.bse.cn/)

## 6. 高性能回测设计

### 6.1 当前问题

当前项目中的回测实现主要存在这些限制：

- Pandas 按股票循环，性能不足。
- 条件表达式能力弱。
- 大结果存 JSON，不利于长期存储和分页读取。
- 回测任务直接用线程，任务可靠性有限。
- 股票池为空时有保护性 LIMIT，不适合生产级全市场回测。
- 指标与策略耦合，不利于复用和缓存。

### 6.2 新回测流程

1. 读取策略版本。
2. 编译 DSL。
3. 分析数据依赖。
4. 通过 DuckDB 从 Parquet 裁剪所需字段和日期切片。
5. 通过 Polars 计算或读取因子矩阵。
6. 批量生成股票池和信号矩阵。
7. 事件驱动撮合。
8. 生成资金曲线、交易明细、持仓快照。
9. 写入 Parquet artifact。
10. 写入 PostgreSQL 摘要。

### 6.3 日线与分钟线策略

第一版支持：

- 日线：全市场回测、选股、调仓、波段信号。
- 周线：趋势过滤、市场状态判断。
- 分钟线：仅对候选股票池做入场/出场确认和更细撮合。

不建议第一版做全市场几十年分钟线全量回测，因为数据量和工程复杂度会明显增加。

#### 日线与分钟线内存交接边界

回测引擎必须分为两个阶段，避免分钟线数据导致 OOM：

1. **阶段一：日线/周线向量化计算**
   - Polars 一次性扫描多年全市场日线与周线 Parquet。
   - 计算股票池过滤、多因子评分、横截面排名、波段信号。
   - 输出每日 Watchlist、目标仓位和候选交易信号。

2. **阶段二：分钟线事件驱动撮合**
   - 引擎按交易日推进。
   - 每天只读取当天 Watchlist 股票的分钟线 Parquet 切片。
   - 分钟线读取必须限制股票池、日期、字段和 bar 数量。
   - 支持懒加载、流式读取和缓存淘汰。
   - 禁止回测启动时一次性加载全市场多年分钟线。

### 6.4 回测 artifact

大结果不要写入 `backtest_jobs.result`。

建议写成：

```text
data/artifacts/backtests/{run_id}/equity.parquet
data/artifacts/backtests/{run_id}/trades.parquet
data/artifacts/backtests/{run_id}/positions.parquet
data/artifacts/backtests/{run_id}/orders.parquet
data/artifacts/backtests/{run_id}/signals.parquet
data/artifacts/backtests/{run_id}/trade_snapshots.parquet
data/artifacts/backtests/{run_id}/metrics.json
```

`trade_snapshots.parquet` 是策略自进化的关键数据。每次 entry 信号和真实买入成交时，必须写入该时刻的核心特征快照：

- `signal_date`
- `fill_date`
- `symbol`
- `side`
- `factor_vector`
- `rank_features`
- `market_state`
- `industry_state`
- `entry_reason`
- `exit_reason`
- `future_return_labels`

API 分页读取：

- `/v1/backtests/{id}/equity`
- `/v1/backtests/{id}/trades`
- `/v1/backtests/{id}/positions`
- `/v1/backtests/{id}/signals`
- `/v1/backtests/{id}/trade-snapshots`

## 7. 仓位管理设计

### 7.1 仓位模型

支持以下模型：

1. **等权模型**
   - 所有入选股票等权。

2. **因子加权模型**
   - 分数越高，权重越高。

3. **波动率目标模型**
   - 波动率越高，仓位越低。

4. **ATR 风险预算模型**
   - 根据止损距离和单笔风险预算计算买入数量。

5. **金字塔加仓模型**
   - 盈利达到阈值后加仓。

6. **分批建仓模型**
   - 初始仓位、确认仓位、突破加仓。

### 7.2 风控拦截

风控应在下单前统一判断：

- 是否超最大持仓数。
- 是否超单票仓位。
- 是否超行业仓位。
- 是否超组合仓位。
- 是否触发回撤熔断。
- 是否触发单日亏损限制。
- 是否处于冷却期。
- 是否涨停无法买入。
- 是否跌停无法卖出。

## 8. 策略自进化设计

### 8.1 进化目标

提升策略综合表现，而不是单纯提升收益率。

目标函数建议综合：

- 年化收益
- 最大回撤
- Calmar
- Sharpe
- 胜率
- 盈亏比
- 交易次数
- 换手率
- 稳定性
- 容量
- 策略复杂度

### 8.2 进化方式

支持：

- 参数搜索
- 因子权重优化
- 因子增删
- 阈值优化
- 出场规则优化
- 风控叠加
- 市场状态分层
- 交易特征归因

#### AI 归因进化

传统参数搜索容易遇到维度灾难，因此进化模块必须显式使用 Trade Snapshot 做归因式进化：

1. 回测时记录每笔买入交易发生时的因子向量和市场状态。
2. 回测后拆分盈利 Top 20% 交易和亏损 Bottom 20% 交易。
3. 使用轻量机器学习模型提取“高质量交易”的共性特征。
4. 自动生成可解释进化建议，例如：
   - 增加资金流强度过滤。
   - 提高行业相对强度阈值。
   - 排除高波动低质量交易。
   - 调整 RSI、ATR、突破阈值。
5. 所有建议必须反向生成 DSL 变更草案。
6. 变更草案必须重新回测、样本外验证，并由用户确认后才能激活。

第一版建议使用 `scikit-learn` 做特征重要性和简单分类/回归归因；`LightGBM` 可作为后续增强，不作为首版硬依赖。

### 8.3 防过拟合规则

必须执行：

- 样本内训练。
- 样本外验证。
- 最终测试集。
- Walk-forward。
- 最小交易次数。
- 多市场阶段评估。
- 复杂度惩罚。
- 与原策略对比。

### 8.4 激活流程

自进化候选策略不能自动覆盖原策略。

流程：

1. 生成候选策略。
2. 回测候选策略。
3. 生成对比报告。
4. 标记风险和过拟合可能。
5. 用户确认。
6. 保存为新版本。
7. 可选择激活或进入纸交易。

## 9. 数据库设计建议

### 9.1 strategy_definitions

策略主表。

核心字段：

- `id`
- `user_id`
- `name`
- `strategy_type`
- `status`
- `current_version_id`
- `source`
- `created_at`
- `updated_at`

### 9.2 strategy_versions

策略版本表。

核心字段：

- `id`
- `strategy_id`
- `version`
- `parent_version_id`
- `dsl`
- `compiled_hash`
- `compile_status`
- `change_summary`
- `created_at`

### 9.3 backtest_runs

回测任务表。

核心字段：

- `id`
- `strategy_id`
- `strategy_version_id`
- `status`
- `progress`
- `start_date`
- `end_date`
- `frequency`
- `initial_capital`
- `benchmark`
- `artifact_root`
- `metrics`
- `error_message`
- `created_at`
- `started_at`
- `completed_at`

### 9.4 factor_registry

因子注册表。

核心字段：

- `id`
- `name`
- `category`
- `description`
- `formula_dsl`
- `required_fields`
- `default_params`
- `is_active`

### 9.5 evolution_experiments

进化实验表。

核心字段：

- `id`
- `strategy_id`
- `base_version_id`
- `objective`
- `search_space`
- `status`
- `best_candidate_id`
- `created_at`

### 9.6 evolution_candidates

候选策略表。

核心字段：

- `id`
- `experiment_id`
- `dsl`
- `metrics`
- `score`
- `risk_flags`
- `status`
- `created_strategy_version_id`

### 9.7 paper_accounts / paper_orders / paper_positions

纸交易相关表。

用于策略从回测到实盘前的验证。

## 10. API 设计

### 10.1 策略 API

- `POST /v1/strategies/llm-draft`
  - 自然语言生成策略草案。

- `POST /v1/strategies`
  - 保存策略。

- `GET /v1/strategies`
  - 策略列表。

- `GET /v1/strategies/{id}`
  - 策略详情。

- `POST /v1/strategies/{id}/compile`
  - 编译校验。

- `POST /v1/strategies/{id}/versions`
  - 创建新版本。

- `POST /v1/strategies/{id}/activate`
  - 激活策略。

- `POST /v1/strategies/{id}/clone`
  - 克隆策略。

### 10.2 回测 API

- `POST /v1/backtests`
  - 创建回测任务。

- `GET /v1/backtests/{id}`
  - 获取任务状态和摘要。

- `POST /v1/backtests/{id}/cancel`
  - 取消任务。

- `GET /v1/backtests/{id}/metrics`
  - 获取指标。

- `GET /v1/backtests/{id}/equity`
  - 获取资金曲线。

- `GET /v1/backtests/{id}/trades`
  - 获取交易记录。

- `GET /v1/backtests/{id}/positions`
  - 获取持仓历史。

- `POST /v1/backtests/compare`
  - 多回测对比。

### 10.3 进化 API

- `POST /v1/evolution/experiments`
  - 创建进化实验。

- `GET /v1/evolution/experiments/{id}`
  - 获取实验状态。

- `GET /v1/evolution/experiments/{id}/candidates`
  - 获取候选策略。

- `POST /v1/evolution/candidates/{id}/accept`
  - 接受候选策略，生成新版本。

### 10.4 纸交易 API

- `POST /v1/paper/accounts`
  - 创建纸交易账户。

- `POST /v1/paper/accounts/{id}/run-strategy`
  - 运行策略并生成模拟订单。

- `GET /v1/paper/accounts/{id}/orders`
  - 查看订单。

- `GET /v1/paper/accounts/{id}/positions`
  - 查看持仓。

- `GET /v1/paper/accounts/{id}/equity`
  - 查看权益曲线。

## 11. 前端设计

### 11.1 策略管理中心

功能：

- 策略列表
- 策略类型过滤
- 状态过滤
- 绩效快照
- 版本查看
- 克隆策略
- 激活/暂停策略
- 发起回测
- 发起进化

### 11.2 AI 创建策略

流程：

1. 输入白话需求。
2. 生成结构化策略。
3. 展示待确认项。
4. 展示 DSL 预览。
5. 展示数据依赖。
6. 展示风险提示。
7. 保存草稿或保存并回测。

### 11.3 策略编辑器

支持两种模式：

- 可视化 Builder。
- DSL JSON 预览。

第一版不需要用户手写复杂代码。

### 11.4 回测工作台

功能：

- 策略版本选择。
- 回测区间。
- 股票池。
- 周期选择。
- 初始资金。
- 费用配置。
- 撮合方式。
- 任务进度。
- 资金曲线。
- 回撤曲线。
- 月度收益。
- 交易明细。
- 持仓变化。
- 诊断信息。

### 11.5 进化工作台

功能：

- 选择基础策略。
- 选择进化目标。
- 设置搜索空间。
- 查看候选策略。
- 样本内/样本外对比。
- 接受候选。
- 进入纸交易。

## 12. 实施阶段

### Phase 1：数据与模型基础

- 引入 Alembic。
- 新建策略版本、回测任务、artifact、进化实验、纸交易表。
- 增加 `duckdb`、`pyarrow`、`polars`、`scikit-learn` 依赖。
- 建立 Parquet 数据缓存目录。
- 将 `stock_daily_kline` 导出为 Parquet 分区。
- 建立 A 股规则配置。

### Phase 2：DSL 与编译器

- 定义 DSL schema。
- 实现 Pydantic 校验。
- 实现 AST 编译器。
- 实现 DSL 到 Polars Expr 的编译后端。
- 实现数据依赖分析。
- 实现未来函数检测。
- 实现核心指标和因子算子。
- 替换当前字符串条件解析器。

### Phase 3：高性能回测引擎

- 实现 DuckDB 数据裁剪与 Polars 因子计算。
- 实现信号矩阵生成。
- 实现事件驱动撮合器。
- 实现分钟线懒加载生成器。
- 实现 A 股规则撮合。
- 实现 artifact 写入。
- 实现指标计算。
- 实现结果分页 API。

### Phase 4：LLM 策略创建与前端

- 实现 LLM 草案接口。
- 实现策略解释报告。
- 实现前端 AI 创建策略页面。
- 实现策略版本管理。
- 实现回测工作台升级。

### Phase 5：自进化与纸交易

- 实现参数搜索。
- 实现因子权重优化。
- 实现 Trade Snapshot 归因分析。
- 实现候选策略评估。
- 实现用户确认激活。
- 实现纸交易账户、订单、持仓。

## 13. 测试计划

### 13.1 DSL 测试

- 白话示例能生成合法 DSL。
- LLM Structured Outputs 必须满足 Pydantic JSON Schema。
- 非法字段返回明确错误。
- 未知因子返回待确认项。
- 未来函数被拦截。
- 多周期规则不能读取未完成周期。
- Alligator、突破、回踩、交叉、分位数、排名结果可复现。

### 13.2 A 股规则测试

- 买入数量按 100 股取整。
- 当日买入不能当日卖出。
- 涨停不能买入。
- 跌停不能卖出。
- 停牌不能成交。
- 佣金、印花税、滑点计算正确。
- 不同市场涨跌幅从配置读取。

### 13.3 回测正确性测试

- 同一策略同一数据多次运行结果一致。
- 无交易场景正常返回。
- 全现金场景正常返回。
- 多票调仓正确。
- 止损止盈正确。
- 熔断降仓正确。
- artifact 可分页读取。

### 13.4 性能测试

- 全市场日线 20 年级别扫描建立 benchmark。
- 200 只股票 1 年分钟线回测建立 benchmark。
- 分钟线必须按 Watchlist 懒加载，不允许一次性加载全市场分钟线。
- 批量候选策略进化建立 benchmark。
- 新引擎必须显著快于旧 pandas 循环实现。
- 大结果禁止写入单个巨大 JSON 字段。

### 13.5 LLM 与进化测试

- LLM 不生成可执行代码。
- 同一白话需求能稳定生成结构相近的 DSL。
- 每笔买入交易都有 Trade Snapshot。
- Top/Bottom 交易归因能输出可解释进化建议。
- 候选策略不能覆盖原策略。
- 样本内提升但样本外退化的候选必须标记过拟合风险。

## 14. 默认假设

- 第一版选择“生产级核心”，不追求一次性完成完整实盘自动交易。
- 第一版支持日线 + 分钟线，但分钟线只用于候选股票池和指定区间。
- 第一版使用 PostgreSQL + Parquet + Polars + DuckDB。
- 第一版 LLM 生成可解释 DSL，不生成可执行 Python。
- 第一版实盘边界为信号 + 纸交易。
- 第一版自进化必须用户确认后才激活。
- “资金 100 亿到 200 亿”默认解释为流通市值。
- “业绩暴增”默认解释为净利润同比高增长。

## 15. 优先实现清单

建议按以下顺序推进：

1. 建立新表结构和 Alembic 迁移。
2. 建立 Strategy DSL schema。
3. 实现 DSL 编译与校验。
4. 实现 Parquet/DuckDB/Polars 数据读取与计算层。
5. 实现日线全市场选股回测。
6. 实现 A 股撮合规则。
7. 实现仓位和风控模型。
8. 实现多周期波段交易。
9. 实现分钟线入场/出场确认。
10. 实现 LLM 策略草案生成。
11. 实现回测 artifact 存储和分页 API。
12. 实现前端 AI 创建策略和回测工作台。
13. 实现 Trade Snapshot 交易特征快照。
14. 实现策略进化实验。
15. 实现纸交易。
