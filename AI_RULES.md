# AI Rules

本文件只记录长期有效规则。临时进度、一次性结论、未验证猜测写入 `AI_PROGRESS.md`，不要写入本文件。

## 新对话启动流程

1. 先读 `README.md`。
2. 再读 `AI_PROGRESS.md`。
3. 最后读 `AI_RULES.md`。
4. 开始改代码前先看 `git status --short`，不要覆盖用户或其他 AI 的未提交改动。

## 每次收尾流程

1. 更新 `AI_PROGRESS.md`，写清本次做了什么、改了哪些文件、验证结果、风险和下一步。
2. 如果规则或长期口径发生变化，更新 `README.md` 或 `AI_RULES.md`。
3. 告诉用户实际改动和验证结果。

## 项目长期口径

- 数据库以 PostgreSQL 为准，不新增 SQLite 依赖或 SQLite 文档口径。
- QMT 账号配置走设置页/数据库，不把账号写死在 `.env`。
- 实盘仓只读；虚拟仓用于模拟交易、策略联调和自动交易验证。
- 实时监控必须是后端后台服务能力，不能依赖前端页面打开。
- 同一监测 K 线内交易指令只能触发一次，轮询不能造成重复下单。
- 页面要展示数据可信度：数据源、更新时间、缓存/实时状态、后台同步状态和异常信息。

## 命名和文件偏好

- 新增 AI 交接类文件统一放项目根目录，使用 `AI_*.md` 命名。
- 用户面向文档优先使用中文 Markdown。
- 代码、配置键、接口路径、表名、命令使用英文或项目已有命名。
- 不在文档中写入真实密码、token、数据库连接串、bridge token。

## 固定工作流

- 搜索文件优先使用 `rg` 或 `rg --files`。
- 手工编辑文件使用 `apply_patch`。
- 前端改动后至少运行 `npm run build`。
- 后端 Python 改动后至少运行相关 `pytest`，必要时加 `python -m py_compile`。
- 涉及页面显示的问题，构建通过后用浏览器实际验证。
- 涉及 QMT、交易、实盘、安全边界的改动，优先补测试。
- 不要执行 `git reset --hard`、`git checkout --` 等破坏性命令，除非用户明确要求。

## UI 和产品规则

- 业务页面应直接展示可用工作台，不做营销式落地页。
- 对实盘、自动交易、数据源、缓存和风控状态，文案要明确，不要让用户误解系统正在真实执行或已经连接。
- 复杂策略、回测、实时监控结果要尽量给出可解释原因，不只展示结论。
